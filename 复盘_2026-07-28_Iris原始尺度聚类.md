# Iris 原始尺度聚类复盘

## 任务信息

- 题目/项目：Iris 数据集三种聚类方法比较，不进行标准差转换
- 日期：2026-07-28
- 数据范围：UCI Iris 修订版，150 个样本、4 个 cm 测量特征、3 个外部物种标签
- 最终交付：Quarto 源文件与嵌入资源的 HTML 报告

## 问题与模型链

- 任务类型：无监督聚类、簇数选择与外部一致性评价
- 最终模型链：原始数据审计 → 原始厘米特征 → K-means、Ward、DBSCAN → WCSS、CH、轮廓系数 → ARI、MI、NMI、AMI → 参数敏感性
- 子模型之间的输入输出：三种算法均直接接收相同的四维原始厘米数据；真实标签只在参数确定后用于外部评价
- 选择理由：K-means 为平方欧氏基线，Ward 提供层级结构，DBSCAN 补充密度与噪声视角

## 最终结果

- 关键数值：
  - K-means：\(K=3\)，WCSS 78.8514，CH 561.628，轮廓系数 0.5528，ARI 0.7302
  - Ward：\(K=3\)，WCSS 79.2971，CH 558.058，轮廓系数 0.5543，ARI 0.7312
  - DBSCAN：\(\varepsilon=0.55\)、MinPts \(=8\)，2 个有效簇、15 个噪声点、覆盖率 90%
- 相对基线提升：不适用；三种方法承担不同结构假设，DBSCAN 的内部指标只基于非噪声样本，不能直接与全样本方法排名
- 不确定区间：未进行独立样本或 bootstrap 区间估计
- 最终建议：综合 WCSS 肘部和 CH 选择 \(K=3\)，同时保留轮廓系数支持的 \(K=2\) 作为合理候选，不声称存在唯一真实簇数

## 验证证据

- 数据外验证：无；物种标签来自同一数据，只能作为样本内外部一致性参照
- 残差/误差：聚类任务不适用
- 敏感性：K-means 与 Ward 的轮廓系数在 \(K=2\) 最大，而 CH 与 WCSS 肘部支持 \(K=3\)；DBSCAN 在 \(\varepsilon=0.50,0.55,0.60\) 时覆盖率和噪声数明显变化
- 稳健性：K-means 在 20 个随机种子下相对基准划分的 ARI 均值为 1
- 极端情景：DBSCAN 的 \(\varepsilon=0.50\) 仅覆盖 82.67% 样本，未通过预设 90% 覆盖率门槛

## 有效做法

1. 在代码中直接将数值表复制为分析矩阵，明确不调用任何缩放器。
2. 将数据检查与数据修改区分开，保留缺失数为零和一条重复记录的事实，不静默清洗。
3. 当内部指标冲突时报告候选结构及其解释，不用单一指标制造唯一答案。

## 失败尝试

| 尝试 | 失败表现 | 原因 | 何时可能有效 |
|---|---|---|---|
| 沿用标准化报告的 DBSCAN 参数 | 参数与原始尺度距离不匹配 | 距离单位和邻域半径的含义已经改变 | 输入仍使用同一标准化方案时 |
| 用短同步等待渲染嵌入资源 HTML | Quarto 进程超过默认等待时间 | Jupyter 执行和资源嵌入需要更长时间 | 关闭资源嵌入或报告较短时 |

## 最敏感与最危险的假设

- 参数/输入：特征尺度、\(K\)、DBSCAN 的 \(\varepsilon\) 与 MinPts
- 假设：四个同为 cm 的测量变量可按原始数值直接构造欧氏距离
- 失效后影响：花瓣长度因标准差较大而获得更高隐含权重，可能改变簇边界、内部指标和推荐簇数

## 可复用资产

- 代码：K-means/Ward 的 \(K=2,\ldots,10\) 指标循环，DBSCAN 覆盖率约束网格
- 数据处理：只审计、不变换的原始数据入口
- 图表：三指标选簇图、Ward 树状图、四面板聚类结果图
- 文字：指标冲突时的审慎结论模板
- 参数：K-means 使用 k-means++、50 次初始化和多随机种子验证

## 下次首选路线

1. 在建模前明确“原始尺度”是否代表业务权重，而不把不标准化视为中性选择。
2. 同时报告原始尺度和至少一种合理替代尺度的结果；若任务禁止预处理，则将替代尺度放在独立敏感性附录。
3. 若需要证明可迁移性，增加 bootstrap/子采样稳定性或独立新样本验证。

## 应更新的知识库文件

- `26_聚类分析严谨方法卡.md` 已覆盖“标准化不是机械步骤”和尺度敏感性，本次无需修改原专题。
