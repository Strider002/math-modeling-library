# Expectorpatro/math 全站知识映射与使用边界

审计对象：<https://expectorpatro.github.io/math/>。  
首次全站审计日期：2026-07-30；最近增量复核日期：2026-08-21。  
用途：确保网站每个与数学建模有关的章节都有本地去向，并阻止草稿、公式错误或经验规则
直接进入模型。它是特定来源的导航和风险索引，不代替各专题方法卡，也不再作为所有建模任务的
默认必读；一般任务按 `00B_任务路由与最小读取集.md` 调用。

## 1. 版本与审计口径

当前把仓库主分支固定到不可变提交
`7f8c177b6baa1010d3199bb1b32119bbe18e40f8`（提交时间 2026-08-18T12:29:46Z）。
它比上次全站发布物字节审计所锁定的
`df67be9e166221776f96883a5f2cd42df2a73f49` 前进 5 个提交；GitHub compare 返回 99 个变更路径。

2026-08-12 对旧锁定提交的逐字符/逐字节口径分为三层：

- 对 186 个 UTF-8 文本文件逐字节读取，合计 2,690,769 bytes、2,261,697 个 Unicode 字符、
  57,862 行，解码错误为 0；
- 对 sitemap 的 52 个页面逐一下载，均为 HTTP 200，且与固定提交的发布 HTML 逐字节相同；
- 对 `html/site` 中除两个部署控制标记外的 121 个公开文件逐一线上下载，121/121 与提交快照
  字节完全一致，公开文件清单 SHA-256 为
  `6E4F842EFB9370EE5E61D60565518EFBEB9CA5774648D159AD9ACF202C284678`。

机械一致性只能证明“读到的是哪一版”和“没有漏文件”，不能证明全部数学正确。2026-08-21
对上述 5 个提交作不可变 compare，并人工复核建模相关实质源文件；生成 HTML、样式、搜索索引和
依赖锁文件不冒充新知识。旧版未变化章节继续沿用既有全章审计和独立核验结论。当前提交的增量
证据、代码静态审查和错误隔离见
`sources/自动学习/2026-08-21_GitHub算法资源与C题工作流复核.md`；旧版发布物字节证据见
`sources/自动学习/2026-08-12_Expectorpatro全站独立版本核验.md`。本轮没有声称再次把当前线上
121 个发布文件逐字节下载比对。

## 2. 证据规则

1. 网站属于 D 级二次资料，只提供结构和待核验线索。
2. 定义、公式、阈值、分布、收敛性和算法性质必须由教材、原始论文、官方文档或复算支持。
3. 网页中的 `info/unsure/change/improvement` 批注、未完成证明、空小节和个人意见一律隔离。
4. 基础证明不逐字复制；本库保存建模中需要调用的结论、条件、失败模式和数值验证。
5. 每个具体任务仍需结合数据和软件版本核验，不能因为本文件列出就机械套用。

## 3. 第 1 章：线性空间

网站知识点：

- 数域、线性空间公理、零元与负元；
- 线性组合、张成、线性相关/无关、极大无关组；
- 基、维数、坐标和换基；
- 子空间、和、交、直和、补空间、商空间；
- 同构、标准映射；
- 线性映射的核、像、秩—零化度、矩阵表示；
- 线性变换、不变子空间、分解、对角化、零化多项式、幂零变换；
- 投影变换；
- 环、一元多项式、整除、公因式、因式。

建模调用：

- 可识别性看核空间，表达能力看列空间，参数自由度看维数；
- 坐标变换必须同步变换参数和解释；
- 直和/投影用于信号分解、最小二乘和状态分解；
- 多项式结构用于特征多项式、差分方程和插值。

本地去向：`19_数学基础与统计理论补充.md`、`27_主成分分析严谨方法卡.md`。
不把抽象代数证明作为比赛必读正文。

## 4. 第 2 章：矩阵

网站知识点：

- 矩阵加减、数乘、乘法、转置/共轭转置、初等变换、迹；
- 排列、行列式、秩、逆、正交/酉矩阵；
- 线性方程的行化简、相容性秩判别、齐次/非齐次解集、Gauss 消元；
- 相抵、相似、合同；
- 广义逆与 Moore–Penrose 逆；
- 特征值、几何/代数重数、对角化；
- Hermitian/实对称谱分解、Rayleigh 商；
- 二次型、惯性、正定/半正定；
- 幂等阵、正交投影、Householder 反射、Givens 旋转；
- LU、Cholesky、QR、SVD。

建模调用：

- 不显式求逆；最小二乘优先 QR/SVD；
- Cholesky 只用于对称正定；SVD 用于秩亏与条件数；
- \(A^+\) 给出最小二乘中的最小范数解；
- 特征分解必须区分一般方阵、实对称和复 Hermitian。

网页隔离问题：

- 复 Hermitian 对角化所用 \(Q\) 应称酉矩阵，不能称正交矩阵；
- 部分证明把复数域、实数域、行/列特征向量和转置/共轭转置混写；
- Cholesky“约为 LU 一半”只能在特定算法和计数口径下作复杂度比较。

本地去向：`19`、`16`、`29`。

## 5. 第 3 章：度量空间

网站知识点：

- 开球、内点、聚点、边界、开闭集；
- 稠密、稀疏、可分、全有界、准紧；
- Cauchy 列、完备化、紧性和有限覆盖；
- \(\mathbb R^n\) 中的收敛、极限点和级数；
- 映射极限、连续、一致连续、压缩映射；
- 紧集上的连续函数、连通与介值；
- 大 \(O\)、小 \(o\)；
- 范数、赋范空间、算子连续/有界、有限维范数等价；
- Hilbert 空间、正交系、Schmidt 正交化、Riesz 表示。

建模调用：

- 压缩映射给固定点存在唯一性和迭代误差界，但必须验证完备空间、自映射与压缩常数；
- 紧性常用于证明最优解存在，不等于算法能找到；
- 有限维范数等价只保证拓扑等价，不保证数值条件数或距离排序相同；
- \(O/o\) 结论必须写明趋近变量和极限区域。

网页状态：存在多处“未完成/有空证明/没搞明白”，故本章只作为概念目录。
本地去向：`16`、`19`、`05`。

## 6. 第 4 章：微积分

网站知识点：

- Fréchet 微分、链式法则、高阶微分；
- Gâteaux 微分及其与 Fréchet 微分的关系；
- 偏导、Jacobian、Hessian；
- 有限增量、Taylor 公式和余项；
- 极值必要/充分条件、梯度；
- Riemann 型多维积分、可积条件、可积函数类和积分性质。

建模调用：

- Gâteaux 方向导数存在不自动推出 Fréchet 可微；
- \(\nabla f=0\) 只是无约束内点的一阶必要条件；
- Hessian 正定/半正定的结论要区分局部严格极小、凸性和退化方向；
- Taylor 近似必须说明阶数、邻域、光滑性与余项；
- 数值积分另按误差容限验证。

本地去向：`05`、`16`、`19`。

## 7. 第 5 章：概率测度

网站知识点：

- 集合运算、\(\pi\) 系、环/域、\(\lambda\) 系、\(\sigma\) 域及生成；
- Borel \(\sigma\) 域；
- 测度、外测度、扩张、完全化、Lebesgue–Stieltjes/Lebesgue 测度；
- 可测映射、简单函数、可测函数；
- a.e.、几乎一致、依测度收敛及关系；
- 非负简单函数、非负可测函数、一般可积函数的 Lebesgue 积分；
- 符号测度、Hahn/Jordan 分解、Radon–Nikodym 导数、Lebesgue 分解；
- \(L_p/L_\infty\) 与收敛；
- 乘积 \(\sigma\) 域、乘积测度和 Fubini/Tonelli。

建模调用：

- 交换极限与积分分别检查单调收敛、Fatou、控制收敛等条件；
- Fubini 与 Tonelli 的非负/绝对可积条件不同；
- 条件期望、似然比、重要抽样和密度变换依赖可测性与绝对连续；
- “几乎处处”不等于每一点。

网页状态：乘积空间、积分加法、\(L_p\) 等处存在明确未完成内容，不能作为完整教材。
本地去向：`07`、`19`。

## 8. 第 6 章：概率初步

网站知识点：

- 随机变量分类与独立性；
- 期望、条件期望、矩、协方差、方差、MSE、相关；
- 随机二次型均值/方差；
- MGF、CGF、特征函数；
- Mahalanobis 距离；
- 二项、负二项、超几何、Poisson、均匀、正态、卡方、t、F、Gamma、Beta；
- 多元/矩阵正态、正态二次型；
- 逆变换等随机数生成；
- Wishart、Hotelling \(T^2\)、Wilks 分布。

建模调用：

- 分布名称必须绑定参数化、支持集、独立性和抽样机制；
- 相关系数只度量相应线性/单调结构且不等于因果；
- Mahalanobis 距离要求稳定的协方差逆，高维需收缩或降维；
- MGF 可能不存在，特征函数总存在；
- 矩阵正态与 Wishart 的维数、自由度和尺度约定必须逐项核验。

本地去向：`07`、`14`、`19`、`28`。

## 9. 第 7 章：渐进理论

网站知识点：

- 几乎必然、依概率、依分布收敛及连续映射；
- 大数定律；
- 中心极限定理；
- 离散到连续的连续性校正；
- Delta method。

建模调用：

- 每个渐近结论写明独立同分布、矩条件、依赖条件和标准化；
- Slutsky/连续映射允许的代入不等于任意非连续变换；
- 连续性校正是离散分布正态近似的有限样本修正，不是精确等式；
- 小样本、重尾、强依赖、边界参数使用 bootstrap、精确或仿真校准。

网页完成度极低，且特征函数证明留有待办。本地去向：`07`、`11`、`19`。

## 10. 第 8 章：凸集与优化

网站知识点：

- 直线、线段、凸集、仿射集、锥、超平面/半空间、多面体、球/椭球；
- 凸函数的一阶/二阶判据；
- 光滑与非光滑最优性、次梯度；
- 上下半连续；
- 回溯、Armijo、Goldstein、Wolfe、强 Wolfe、Zoutendijk；
- 梯度下降的基本迭代式与学习率；
- Newton 法及局部二次收敛；
- 新增模拟退火与遗传算法的结构和 Python 演示。

建模调用：

- 局部最优即全局最优只对凸目标+凸可行域；
- 有约束最优使用法锥/KKT，并检查约束资格；
- 线搜索条件不单独保证算法收敛；
- Hessian 可逆不等于 Newton 二次模型有最小值。

网页隔离：多处证明仍有待办，Newton 文字把“解驻点方程”直接说成“最小化二次模型”；现行新增
梯度下降只给算法定义，不能据此声称任意正步长收敛，仍需光滑性、步长和目标结构等条件。当前
凸集增量还含 Minkowski 和左侧下标误写、多面体维数不相容、半空间证明把
\(\theta\in[0,1]\) 放宽为 \(\theta\ge0\)，以及把 \(\lVert u\rVert_2^2\) 写成
\(\lVert u\rVert_2\) 的椭球证明错误。

模拟退火简化接受率只在对称提议或明确作为有限预算启发式规则时可直接使用；非对称提议若要维持
Boltzmann 目标分布需提议比。仅有 \(T_k\downarrow0\) 也不是全局收敛充分条件。遗传算法需区分
个体级/基因级变异概率，并处理精英后子代数为奇数的情况。两个 notebook 只完成静态代码审计；
本地缺少 `simanneal` 与 `deap`，不得声称已执行复现。正确调用边界见 `25`。
本地去向：`05`、`25`、`19`。

## 11. 第 9 章：统计初步

网站知识点：

- 统计结构与统计量；
- 次序统计量及联合分布；
- 充分、极小充分、完全统计量；
- 指数族；
- 单变量和多变量抽样分布；
- 多元正态均值检验与两总体比较。

建模调用：

- 充分性/完全性服务于指定参数模型，不代表模型本身正确；
- 次序统计量和分位数要声明连续性与并列值；
- Hotelling \(T^2\) 等多元检验要求维数、样本量、协方差可逆及分布条件；
- 高维 \(p\ge n\) 不能直接套经典逆协方差检验。

网页完成度标记很低且多处证明未完成。本地去向：`07`、`19`。

## 12. 第 10 章：点估计

网站知识点：

- 风险函数、无偏估计、UMRU/UMVUE；
- Rao–Blackwell、Lehmann–Scheffé；
- Cramér–Rao/信息不等式；
- 矩估计、极大似然、常见均值/方差估计；
- 区间估计和样本量。

建模调用：

- estimand、estimator、estimate 分开；
- \(MSE=\operatorname{Var}+\operatorname{Bias}^2\)；
- CR 下界和 MLE 渐近性质依赖正则性、可识别、内点和信息有限；
- 区间覆盖是重复抽样性质；
- 样本量必须基于目标效应、方差、功效/精度、设计效应和损耗率。

本地去向：`07`、`11`、`19`。

## 13. 第 11 章：假设检验

网站知识点：

- 第一/二类错误、势函数、随机化检验；
- FWER、FDR、Bonferroni、BH、BY；
- 单样本符号、Wilcoxon 符号秩；
- Cox–Stuart 趋势和游程/随机性类非参数检验。

网页隔离：

- 至少一次第一类错误的 \(1-(1-\alpha)^n\) 少写“所有原假设为真、独立且大小恰为
  \(\alpha\)”；
- 真原假设数与假原假设数都写成 \(n_0\)；
- 符号检验并列值处理加入个人意见，且双边 p 值留有争议批注；
- 趋势检验势函数/近似性质未解决。

本地去向：`07_概率统计与随机模型.md`，以该文件的 ties、多重性和前提说明为准。

## 14. 第 12 章：贝叶斯统计

网站知识点：

- 群与 location/scale 族；
- 指数族、共轭先验；
- posterior、posterior predictive、HPD/等尾区间；
- Beta–Binomial、Normal–Normal、Inverse-Gamma–Normal、Gamma–Poisson；
- Jeffreys、位置/尺度非信息先验；
- posterior mean/median/mode 点估计。

网页隔离：

- location family CDF 中存在 `y_2-y_2` 下标错误；
- 贝叶斯分析流程正文未完成；
- Normal–Normal 推导一处后验均值分母漏掉 \(n\)；
- 自然共轭指数族写法不完整；
- 多参数 location–scale 先验被过度简化；
- MAP 被称为“广义最大似然”，忽略参数化和损失函数。

本地去向：`07`；正式贝叶斯任务另核对 Stan/PyMC 等所用版本和后验诊断。

## 15. 第 13 章：统计计算

网站知识点：

- 二分、不动点、Newton 求根；
- Lagrange、Newton、Hermite 插值；
- 变分下界与 EM。

建模调用：

- 二分需要连续和异号括号；
- 固定点需要自映射/压缩等收敛条件；
- Newton 需要导数、初值和保护；
- 高次全局插值有 Runge 与浮点不稳；
- EM 保证观测似然非降，不保证全局最优；变分法通常只优化下界。

现行网页 EM 的抽象 ELBO/KL 坐标上升表述不能替代支持条件、精确/广义 EM 区分、停止规则与
退化诊断；本地以 `33_EM算法与潜变量模型严谨方法卡.md` 为准。用户附件 `EM.pdf` 与
`D:\数学建模\EM.pdf` 字节完全相同，但不是现行网页源码的同版正文。

本地去向：`16`、`19`、`33`。

## 16. 第 14 章：线性模型

网站知识点：

- 一般/正态线性模型、OLS、约束最小二乘、误差方差与预测；
- GLS 和统一最小二乘；
- QR/SVD 数值计算；
- 中心化、标准化、F/t 检验、\(R^2\)；
- subset/forward/backward/stepwise；
- 残差、Box–Cox、Cook、异常点、共线性、VIF、Ridge；
- 单/双因素 ANOVA；
- GLM、Logistic、Poisson、Newton/IRLS。

网页隔离：

- 标准化系数被直接解释为预测重要性；
- VIF 被错误定义为 \((X^\top X)^{-1}_{jj}\)；
- 条件数 100/1000 被写成固定等级而未说明尺度和矩阵；
- 内部学生化残差被近似称为相互独立；
- 任意平移正响应后 Box–Cox 的影响未说明；
- 步进显著性没有选择后推断警告；
- Ridge 启发式的 `max` 与其自身不等式方向矛盾；
- GLM 的统一 Newton 方程混淆工作响应与参数增量。

2026-08-12 现行版虽重写了一般线性模型、数值计算与 ANOVA 的大段正文，但上述八项在相应现行
源码中仍可定位，不因重写自动撤销。特别是 VIF、条件数分级、Hoerl--Kennard 的 `max` 以及
`X^\top W Xz=X^\top(y-\mu)` 均仍存在；正式任务继续以本地方法卡为准。

本地去向：`29_线性模型与广义线性模型严谨方法卡.md`。

## 17. 第 15 章：多元统计

网站知识点：

- PCA、样本 PCA、相关矩阵 PCA、PCR；
- 因子模型、提取、旋转、检验、因子得分；
- NMF 的 Frobenius/KL 目标；
- CCA；
- 距离/Bayes/Fisher 判别；
- 聚类指标、K 系列、层次、DBSCAN、OPTICS。

网页的全部已知公式和边界问题已分别隔离在：

- `27_主成分分析严谨方法卡.md`；
- `28_多元统计扩展严谨方法卡.md`；
- `26_聚类分析严谨方法卡.md`。

2026-08-14 的 PCA 重构再次出现确定问题：变量表示质量公式一处分母写成
\(\sigma_{ii}\)，应绑定被解释变量 \(X_j\) 的 \(\sigma_{jj}\)；算法同时采用
\(\Sigma=A\Lambda A^\top\)、`A` 的前 \(k\) 行和 `Y=A_kZ`，没有统一特征向量按列与数据方向。
“发现共线性即删除/合并变量”“PCA 无法解决重复信息”和固定 85% 选维均不得作为通用规则。
因此当前仍以 `27` 的 \(n\times p\) 方向、SVD、训练外变换和验证选维为准。因子分析、NMF、
CCA、判别与聚类中的旧风险仍按 `26`、`28` 隔离。

## 18. 第 16 章：时间序列

网站知识点：

- 弱平稳、谱分布/密度、正交与线性预测空间；
- 自协方差/自相关、长短记忆；
- 白噪声、有限/无限 MA、线性过程；
- 差分、位移、线性差分方程；
- AR、MA、ARMA、ARIMA、单位根；
- 均值/自协方差估计；
- 最佳线性/条件期望预测；
- Wold 表示。

网页隔离：

- 一般 ARMA 的 ACF 被写成 \(k>q\) 后为零，同时又称指数拖尾；
- AR 的 ACF 被称为每个滞后“始终不为零”，结论过强；
- ARIMA 模型一处误记为 ARMA\((p,d,q)\)；
- MA 谱密度有限和上下标写成未定义的 \(k\)；
- Wold 证明含明确“未完成”。

本地去向：`04`、`23`、`19`，以滚动回测和残差诊断为最终证据。

当前新增 ARIMA 识别实验完整展示“序列图/ADF → ACF/PACF 候选 → 残差与 Ljung—Box → 预测”。
正文正确说明 Ljung—Box 不证明独立、正态或同方差，且 `p=0.0100` 是 ADF 软件报告下限。该实验
仍是全样本识别与向前预测，不含滚动训练外比较；比赛中不能据此跳过季节朴素基线和滚动回测。

## 19. 第 17 章：机器学习

网站知识点：

- Accuracy、Precision/Recall/F1、ROC/PR、多分类平均、MAE/MAPE；
- 过拟合、训练/验证/测试、正则化；
- 新增模型训练章：随机划分、训练内预处理、K 折交叉验证与基本调参流程；
- 熵/Gini/交叉熵、CART、Bagging、OOB、Boosting、GBDT、XGBoost；
- 直方图/列块/稀疏与缺失处理；
- KNN、KD-tree 与 Ball-tree；
- kernel 与 RKHS；
- 前馈网络、反向传播、RNN、LSTM、GRU、卷积、池化、CNN；
- 新增 BatchNorm、LayerNorm、残差连接和 inverted Dropout；
- 新增决策树、随机森林、GBDT、XGBoost 的训练/验证/测试演示。

网页隔离：

- AUROC 被写成“通常 \([0.5,1]\)”而没有给数学范围 \([0,1]\)；
- AUPRC 未区分 average precision 与插值积分；
- MAE 被说成对单个异常值不敏感，忽略其贡献仍无界；
- impurity importance 未说明取值数偏差；
- 相关 Bagging 错误率仅凭等相关就套普通 CLT；
- OOB 被近似称为独立验证；
- Bagging 未明确观测 bootstrap 是有放回抽样，随机森林特征随机化被写成笼统的
  “特征自助采样”，而标准随机森林通常在每个节点抽取候选特征子集；
- XGBoost 页面定义的是“分裂后目标减分裂前目标”，有利值为负；若称正的 split gain，
  应改用其相反数并以 `Gain > 0` 判断；
- `exact`、`approx`、`hist`、列块预排序、稀疏与缺失处理的描述必须绑定软件版本；
- KD/Ball tree 的高维退化未说明；
- 当前网络定义把所有激活称为逐元素函数，却又把 softmax 纳入同一类；含偏置的复合还被称作
  严格线性映射，且“\(L>2\) 才有隐藏层”的层数判断不成立；
- batch 公式把批索引 \(r\) 误写成 \(i\)，没有完整说明最后一批；
- 梯度裁剪公式本身正确，但“直接为参数变化提供上界”的解释不适用于含动量、Adam 或额外
  权重衰减项的一般优化器；
- LSTM 现代门公式当前自洽，但“解决梯度消失与爆炸”表述过强，cell state 也不能由门值有界
  推出统一有界；
- GRU 正文使用 \(z_t\) 保留旧状态，Algorithm 17.8 却把新旧系数反转，属于确定的公式—伪代码冲突；
- 所谓二维卷积没有翻转核，数学上是互相关；输出尺寸只覆盖 stride=1、dilation=1 的特例；
- 池化降低局部位移敏感性是结构直觉，不保证严格不变性；序列均值/末状态未处理 padding mask；
- 神经网络小节没有可执行神经网络例题代码，页面三个计算实验均为树模型。

2026-08-17/18 增量新增第三个树集成比较 notebook。该实验用单次 60/20/20 划分和验证集调参，
未在训练+验证上
重拟合，分类也未做阈值成本与校准；只能作数据角色演示，不是嵌套验证或模型优越性证据。新增
BatchNorm、LayerNorm、残差和 Dropout 主体公式可用，但归一化轴、运行统计、train/eval 模式、
投影支路和框架版本必须按 `32` 核验。

本地去向：`31_树模型与集成学习严谨方法卡.md`、`32_神经网络严谨方法卡.md`、
`24_机器学习评审标准.md`、`06`、`22`。

训练章的随机划分只适用于交换性近似成立的样本；时间序列、空间、主体或滑窗数据必须改用按时间/
组/空间隔离的验证协议，不能照搬随机 `train_test_split`。当前 KNN 源码 SHA-256 为
`4E2CCE8C216C4082BC901D377AD96AA0BE78FA31B9F8C8CABC6725DC62F80AF7`；当前神经网络源码
SHA-256 为 `6DE4BDF409DA387466921A8EDDD35FF167EBA43D9C005398AF49513312C3856E`。

增量审计说明：2026-07 归档源码中的 `FindBestSplit` 最优分裂赋值方向问题，已在上述
2026-08-02 当前提交中修复，只作为历史版本变更，不再列为当前网页错误。

2026-08-09 对神经网络部分按提交
`df67be9e166221776f96883a5f2cd42df2a73f49` 增量重审。早期版本中 sigmoid 范围和
cell state 有界性的旧问题不再冒充当前原文错误；当前问题、源码哈希、47 个展示公式与数值复核
见 `sources/自动学习/2026-08-09_Expectorpatro神经网络公式代码复核.md`。

2026-08-21 当前提交的增量复核见
`sources/自动学习/2026-08-21_GitHub算法资源与C题工作流复核.md`；旧哈希和旧页面实验数量
不得继续描述成当前版本。

## 20. 第 18 章：因果推断

网站知识点：

- 潜在结果、SUTVA、一致性、可交换性、正值性、ATE；
- g-formula；
- 观察性研究、倾向得分、平衡性质；
- IPW 识别、已知倾向得分下无偏/相合/渐近正态。

网页隔离：

- \(E(Y^a/Y^b)\) 与 \(E(Y^a)/E(Y^b)\) 混淆风险；
- “倾向得分可任意选择模型”忽略平衡、重叠与估计误差；
- 点态正值性未写目标支持集和实际重叠；
- 已估计倾向得分的有限样本偏差、方差和交叉拟合未处理；
- 一处 ATE 期望写成未定义的 \(\tau^{IPW}\)。

本地去向：`17_因果推断与实验设计.md`。

## 21. 附录、术语、参考文献与计算实验

附录知识点：

- Kronecker、vec、交换矩阵、矩阵平方根；
- Cauchy–Schwarz/Rayleigh、Young、Hölder、Minkowski、Jensen、Gibbs、Markov；
- 等价关系和数域。

调用边界：

- 每个不等式写明空间、共轭指数、可积性和等号条件；
- \(A^{1/2}\) 的唯一对称半正定平方根只对半正定矩阵；
- vec/Kronecker 先做维数检查；
- Markov/Chebyshev 给上界，不等于真实尾概率。

计算实验包括 EDA、线性回归/共线性—Ridge、PCA 等。保留的原则是：

- 一行/一列含义、单位、缺失、重复、越界先于作图；
- 极端值不等于错误；
- 不同图若使用不同完整案例必须标样本；
- 所有预处理和模型选择放在训练内；
- 实验代码只作可复现示例，示例数据结论不迁移到新题。

术语表只用于中英名称统一；参考文献页用于定位原始来源，不因“列入参考文献”就自动完成核验。

## 22. 使用该网站线索时的条件路由

只有当任务使用本网站线索、核对其版本/错误，或对应方法卡引用本网站时，才按下表追加读取；
一般建模任务直接按 `00B`，无需先读本文件全文。

| 任务 | 额外必读 |
|---|---|
| 数据清洗/EDA | `02`、`22` |
| 回归/GLM/ANOVA | `29` |
| PCA/PCR | `27` |
| 聚类 | `26` |
| 因子/NMF/CCA/判别 | `28` |
| 预测/时间序列 | `04`、`23` |
| 机器学习 | `06`、`24` |
| CART/Bagging/随机森林/GBDT/XGBoost | `06`、`24`、`31` |
| 感知机/MLP/RNN/LSTM/GRU/CNN | `06`、`24`、`32`；若使用本次 `gru.py`，另读附件审计记录 |
| EM/GMM/潜变量 | `07`、`19`、`33` |
| 优化 | `05`、`25` |
| 因果 | `17` |
| 数值计算 | `16`、`19` |

网站与本库冲突时，以独立核验后的专题卡为准；仍有疑问时暂停引用并回查原始论文、权威教材或
所用软件版本的官方文档。
