# 仿真模型

## 1. 何时使用仿真

当系统包含复杂流程、随机性、拥堵、反馈、多主体行为或难以求解析解时，用仿真回答“如果……会怎样”。

仿真不是自动真实；它把输入假设转化为输出。输入不可信，输出再精细也不可信。

## 2. 仿真类型

### 蒙特卡洛

重复随机抽样，估计概率、期望、分位数和风险。

### 离散事件仿真（DES）

系统状态在事件发生时改变。适合排队、生产、物流、医院流程。

要素：实体、资源、队列、事件、时钟、路由规则。

### Agent-based Model（ABM）

个体有属性、规则、交互和适应。适合异质主体、局部互动和涌现。

### 系统动力学（SD）

用存量—流量—反馈回路描述宏观动态。适合政策、资源、产业与长期反馈。

## 3. 仿真研究流程

1. 明确问题与绩效指标；
2. 建概念模型和系统边界；
3. 收集输入数据并拟合分布；
4. 编码；
5. verification：代码是否按概念模型运行；
6. validation：概念模型是否足够代表现实；
7. 设计实验；
8. 多次重复；
9. 统计输出与不确定性；
10. 提出决策建议。

## 4. 输入分布

不要因为方便就全部设正态分布。

- 到达间隔常检验指数/非齐次 Poisson，但可能过度离散；
- 服务时间常右偏，可考虑 Gamma、Lognormal、Weibull；
- 概率/比例可考虑 Beta；
- 计数可考虑 Poisson/Negative Binomial；
- 有界量可用三角分布或经验分布。

同时建模相关性，独立抽样可能严重低估或高估风险。

## 5. 随机数与重复

- 固定并记录随机种子；
- 不同方案使用 common random numbers 可减少比较方差；
- 运行多次独立重复；
- 报告均值、标准误、置信区间和尾部；
- 根据期望半宽确定重复次数。

## 6. 暖机期与终止

- 终止型仿真：有自然起止，如一天运营；
- 稳态型仿真：需去除初始瞬态，确定暖机期；
- 检查运行长度和自相关；
- 批量均值可估计稳态输出误差。

## 7. 验证

### Verification

- 单元测试；
- 事件跟踪；
- 极端条件测试；
- 守恒检查；
- 与手算小例比较；
- 动画/可视化辅助查逻辑。

### Validation

- 与历史数据比较；
- 专家 face validation；
- 分布、均值、方差和时序形状比较；
- 对保留时段预测；
- 检查模型能否重现已知现象。

## 8. 实验设计

不要一次只改一个参数后就下结论。可使用：

- 全因子/部分因子；
- 拉丁超立方；
- 响应面；
- Sobol 全局敏感性；
- 场景矩阵；
- 仿真优化。

区分可控因素、噪声因素和响应指标。

## 9. 仿真优化

当目标只能通过仿真估计：

- 排名与选择；
- 响应面/代理模型；
- 贝叶斯优化；
- 随机近似；
- 元启发式 + 重复仿真。

同一方案的目标有随机误差，不能用单次运行值比较。

## 10. ABM 特别注意

- 个体规则是否有数据或理论依据；
- 微观参数是否可识别；
- 多组参数可能产生相同宏观现象；
- 校准与验证应分离；
- 报告涌现结果对规则、网络和初始条件的敏感性。

## 11. 常见错误

- 只运行一次；
- 不设暖机期；
- 用同一随机流导致不合理相关；
- 输出很多小数但置信区间很宽；
- 仅让模型拟合一个宏观指标；
- 把仿真动画当作验证；
- 不公开输入分布和规则。
