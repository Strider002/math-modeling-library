interface:
  display_name: "严谨数学建模知识库"
  short_description: "路由严谨建模方法，并检查阶段产物、证据、结果与提交"
  default_prompt: "Use $math-modeling-library to route this modeling task, enforce the required validation gates, and use the project tools when an end-to-end deliverable is requested."

policy:
  allow_implicit_invocation: true
