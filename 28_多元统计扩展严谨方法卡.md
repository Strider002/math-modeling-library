# 多元统计扩展严谨方法卡

适用范围：因子分析、非负矩阵分解（NMF）、典型相关分析（CCA）、线性/二次判别分析（LDA/QDA）
和 Fisher 判别降维。PCA 与聚类分别使用 `27_主成分分析严谨方法卡.md`、
`26_聚类分析严谨方法卡.md`。

本卡只保留经独立核验的内容。Expectorpatro/math 多元统计网页只作为章节线索；
网页中的维数错配、符号错误、缺失逆矩阵、未完成自由度和经验规则不作为证据。

## 1. 共同前置检查

1. 明确数据方向：本文统一 \(X\in\mathbb R^{n\times p}\)，行是样本，列是变量。
2. 划分训练、验证、测试集后再估计均值、方差、协方差、载荷、判别方向和超参数。
3. 连续变量先检查量纲、偏态、极端值和缺失机制；任何标准化参数只由训练集估计。
4. 报告样本量 \(n\)、变量数 \(p\)、有效秩、条件数或最小特征值。协方差奇异或近奇异时，
   不得直接写逆矩阵而不说明广义逆、降维或收缩估计。
5. 分解模型通常存在符号、排列、旋转或尺度不唯一。解释前要固定对齐规则；
   不能把一次算法输出的列序和正负号当成唯一科学结论。
6. 探索性结果必须用重抽样、扰动、留出集或外部变量检验稳定性。

## 2. 因子分析

### 2.1 模型、协方差与识别

经典正交因子模型为

\[
x_i=\mu+\Lambda f_i+\varepsilon_i,\qquad
\operatorname E(f_i)=0,\quad \operatorname{Cov}(f_i)=I_r,
\]
\[
\operatorname E(\varepsilon_i)=0,\quad
\operatorname{Cov}(\varepsilon_i)=\Psi,\quad
\operatorname{Cov}(f_i,\varepsilon_i)=0,
\]

其中 \(\Lambda\in\mathbb R^{p\times r}\)，\(\Psi=\operatorname{diag}
(\psi_1,\ldots,\psi_p)\) 且通常要求 \(\psi_j>0\)。因此

\[
\Sigma=\operatorname{Cov}(x_i)=\Lambda\Lambda^\top+\Psi .
\]

第 \(j\) 个变量的共同度和特殊方差分别为

\[
h_j^2=\sum_{\ell=1}^{r}\lambda_{j\ell}^2,\qquad
\psi_j=\sigma_{jj}-h_j^2.
\]

共同方差总量为
\[
\sum_{j=1}^{p}h_j^2=\operatorname{tr}(\Lambda\Lambda^\top)
=\sum_{\ell=1}^{r}\|\lambda_{\cdot\ell}\|_2^2.
\]

若 \(T\) 为正交矩阵，则 \(\Lambda T\) 给出相同的
\(\Lambda\Lambda^\top\)。所以未加额外约束时载荷只识别到正交旋转；
斜交旋转还允许因子相关，必须同时报告因子相关矩阵。载荷的符号也不唯一。

### 2.2 高斯极大似然的正确对象

若 \(x_i\stackrel{\mathrm{iid}}{\sim}N_p(\mu,\Sigma)\)，以极大似然尺度

\[
S_n=\frac1n\sum_{i=1}^n(x_i-\bar x)(x_i-\bar x)^\top ,
\]

则剖面对数似然（忽略与参数无关常数）为

\[
\ell(\Sigma)=-\frac n2\left[
\log|\Sigma|+\operatorname{tr}\!\left(S_n\Sigma^{-1}\right)
\right],\qquad \Sigma=\Lambda\Lambda^\top+\Psi.
\]

这使用全部样本散布信息，不是只使用样本均值。若软件采用
\(S_{n-1}\) 或不同常数，系数需相应调整，但
\(\log|\Sigma|\) 与 \(\operatorname{tr}(S\Sigma^{-1})\) 的结构不能改变。
任何把 \(\Sigma\) 写成 \(\Lambda\Lambda^\top-\Psi\)，或在迹项中漏掉
\(\Sigma^{-1}\) 的公式都应拒绝。

### 2.3 旋转与因子数

常用正交 Varimax 目标可写为

\[
V(\widetilde\Lambda)
=\sum_{\ell=1}^{r}\left[
\frac1p\sum_{j=1}^{p}\widetilde\lambda_{j\ell}^{4}
-\left(\frac1p\sum_{j=1}^{p}\widetilde\lambda_{j\ell}^{2}\right)^2
\right].
\]

不同标准化约定会整体乘以正常数，但第二项的平方和系数不能再多除一次 \(p\)。
旋转提升“简单结构”可解释性，不会创造新的拟合信息。

因子数不能只靠“累计解释率达到某固定百分比”。至少联合使用：

- 平行分析或重抽样特征值基准；
- 残差相关、拟合统计量和样本外重构/似然；
- 不同因子数下的载荷稳定性与可解释性；
- 专业先验及因子是否有足够且稳定的高载荷指标。

似然比检验只在高斯模型、正则性与可识别条件近似成立时使用；小样本、
高维、边界解和严重非正态时不能机械使用卡方近似。自由度必须由所采用模型的
独立协方差参数数目减去自由参数数目推导，不能保留占位符。

### 2.4 失败模式与验证

- Heywood 情形：估计特殊方差为负或接近零。检查因子数、样本量、异常点、
  模型误设与数值约束，不能把负方差当正常结果。
- 量纲：使用协方差矩阵会受单位影响；使用相关矩阵改变了问题含义。
  “模型形式可换算”不等于“结果不受单位影响”。
- 因子得分不唯一。Bartlett、回归法等是不同预测准则，不应当作观测到的真实因子。
- 至少报告：提取法、旋转法、标准化方式、因子数依据、载荷阈值仅作展示还是作决策、
  共同度/特殊方差、残差相关、重抽样对齐后的载荷稳定性。

## 3. 非负矩阵分解

### 3.1 定义与维数

给定 \(X\in\mathbb R_{+}^{n\times p}\)，选择秩 \(r\)，求

\[
X\approx WH,\qquad
W\in\mathbb R_{+}^{n\times r},\quad
H\in\mathbb R_{+}^{r\times p}.
\]

平方 Frobenius 损失为

\[
\min_{W,H\ge0}\frac12\|X-WH\|_F^2
=\frac12\sum_{i=1}^{n}\sum_{j=1}^{p}
\left(x_{ij}-(WH)_{ij}\right)^2.
\]

广义 KL 损失为

\[
D_{\mathrm{KL}}(X\|WH)
=\sum_{i,j}\left[
x_{ij}\log\frac{x_{ij}}{(WH)_{ij}}-x_{ij}+(WH)_{ij}
\right].
\]

约定 \(0\log(0/y)=0\)；若 \(x_{ij}>0\) 而 \((WH)_{ij}=0\)，该项为
\(+\infty\)。实现时通常用正初始化或数值下界避免除零，但必须说明这属于数值处理。

### 3.2 不唯一与算法边界

对任意正对角矩阵 \(D\)，
\[
WH=(WD)(D^{-1}H),
\]
且因子列可同时置换，所以尺度和排列不唯一。一般 NMF 目标对 \(W,H\)
联合非凸；乘法更新、交替最小二乘或坐标下降通常只能保证达到驻点或局部解，
不能声称得到全局最优。

平方损失和 KL 损失隐含不同误差结构，不应只按方便选择。若数据含负值，
不能在没有含义说明时整体平移后直接解释 NMF 成分。

### 3.3 选秩与验证

- 在训练集上拟合，比较验证集重构误差或任务相关下游指标；
- 多随机初值，报告目标函数分布、收敛状态和成分匹配后的稳定性；
- 检查稀疏性、成分可解释性以及是否被少数大值变量支配；
- 选秩过程属于调参，最终性能必须用未参与选秩的外层验证或测试集评估。

## 4. 典型相关分析

### 4.1 稳健的 SVD 表述

对中心化随机向量 \(X\in\mathbb R^p\)、\(Y\in\mathbb R^q\)，CCA 求

\[
\max_{a,b}\operatorname{Corr}(a^\top X,b^\top Y)
\]

满足 \(a^\top\Sigma_{XX}a=1\)、\(b^\top\Sigma_{YY}b=1\)，并令后续典型变量
与此前典型变量在各自变量组内不相关。

当组内协方差正定时，构造

\[
K=\Sigma_{XX}^{-1/2}\Sigma_{XY}\Sigma_{YY}^{-1/2}
=U\operatorname{diag}(\rho_1,\ldots,\rho_s)V^\top .
\]

则
\[
a_k=\Sigma_{XX}^{-1/2}u_k,\qquad
b_k=\Sigma_{YY}^{-1/2}v_k,
\]
且 \(\rho_k\in[0,1]\) 是典型相关，\(s\le\min(p,q)\) 并受有效秩限制。
等价广义特征值问题的非零特征值是 \(\rho_k^2\)，所以典型相关应取
\(\sqrt{\lambda_k}\)，不是 \(\sqrt{\lambda_k^2}\)。

\(\Sigma_{XX}^{-1}\Sigma_{XY}\Sigma_{YY}^{-1}\Sigma_{YX}\)
一般不是欧氏对称矩阵，不能据此宣称其特征向量必然正交。白化后的 SVD
避免了这一表述错误。

### 4.2 高维偏差与验证

- 当 \(p+q\) 相对 \(n\) 较大时，样本典型相关会严重乐观，协方差也可能奇异；
  使用正则化 CCA、先验降维或稀疏 CCA，并把正则化强度纳入训练内调参。
- 系数受变量尺度、共线性和符号选择影响；同时报告结构相关
  （原变量与典型变量的相关）和冗余度，不能只解释原始系数大小。
- 显著性检验不代替预测验证。应用中应使用置换检验、嵌套交叉验证或独立样本，
  比较样本外典型相关并检验系数/载荷稳定性。

## 5. 判别分析

### 5.1 高斯 Bayes 判别

设类别 \(k\) 的条件分布为
\(X\mid Y=k\sim N_p(\mu_k,\Sigma_k)\)，先验概率为 \(\pi_k\)。
等误分类代价下选择判别分数最大的类别：

\[
\delta_k(x)=
-\frac12\log|\Sigma_k|
-\frac12(x-\mu_k)^\top\Sigma_k^{-1}(x-\mu_k)
+\log\pi_k.
\]

若所有类别共享 \(\Sigma_k=\Sigma\)，二次项中与类别无关的部分可消去，
得到线性边界（LDA）；类别协方差不同时得到二次边界（QDA）。
因此“选 Mahalanobis 距离最近的均值”只有在共享协方差、相同先验和相同误判代价等
条件下才等价。一般 QDA 还必须包含 \(\log|\Sigma_k|\) 与 \(\log\pi_k\)。
若误判代价不等，应最小化条件风险，而不是固定取最大后验概率。

### 5.2 Fisher 判别降维

对 \(K\) 类样本，定义组内与组间散布矩阵

\[
S_W=\sum_{k=1}^{K}\sum_{i:y_i=k}(x_i-\bar x_k)(x_i-\bar x_k)^\top,
\]
\[
S_B=\sum_{k=1}^{K}n_k(\bar x_k-\bar x)(\bar x_k-\bar x)^\top.
\]

判别方向由广义特征值问题
\[
S_Bv=\lambda S_Wv
\]
给出，可用判别子空间维数至多为
\(\min(p,K-1,\operatorname{rank}S_W)\)。多分类问题不能普遍简化为一条轴上
“离哪个投影均值近就分到哪类”；分类规则还取决于先验、协方差与采用的判别模型。

### 5.3 实施与评审

- 类别不平衡时报告先验如何设定，并用与业务代价一致的阈值和指标；
- 小样本高维时优先考虑 SVD 解法、收缩协方差或正则化判别；
- 用分层且成组/按时间正确的外层验证评估，所有标准化、变量筛选、协方差估计和调参
  都必须位于训练折内；
- 报告混淆矩阵、类别指标、概率校准和代价敏感结果，不以训练集类间分离图代替泛化证据；
- 检查条件高斯、共享协方差等假设；若不成立，用 QDA、正则化模型、
  非参数基线或概率分类器作对照。

## 6. 网页线索的已隔离问题

以下内容不得从 Expectorpatro/math 网页原样复制进模型或论文：

1. 因子分析特殊方差总和下标把变量数与因子数混用；
2. 极大似然只基于样本均值、迹项漏 \(\Sigma^{-1}\)、
   \(\widehat\Sigma=\widehat\Lambda\widehat\Lambda^\top-\widehat\Psi\)；
3. Varimax 第二项系数错误及因子数检验自由度占位符；
4. NMF 的 \(H\) 维数和求和上下标不一致；
5. CCA 把 \(\sqrt{\lambda}\) 写成 \(\sqrt{\lambda^2}\)，并把一般非对称矩阵的
   特征向量称为正交；
6. 判别分析距离公式缺类别下标，且省略先验、行列式和误判代价。

## 7. 交付清单

- [ ] 数据方向、尺度、缺失和异常处理已声明；
- [ ] 协方差的秩、条件数与正则化策略已记录；
- [ ] 训练外评估无泄漏；
- [ ] 因子/成分的符号、排列、旋转或尺度不唯一已处理；
- [ ] 因子数、NMF 秩、CCA 正则化和判别超参数均在训练内选择；
- [ ] 至少一个简单基线和一个合理替代模型；
- [ ] 重抽样或扰动稳定性结果已报告；
- [ ] 不把解释率、显著性或训练集分离度当作预测性能；
- [ ] 所有公式、软件版本和数据来源可追溯。

## 8. 主要核验来源

- Harman, *Modern Factor Analysis*；Lawley & Maxwell, *Factor Analysis as a Statistical Method*。
- Kaiser (1958), Varimax criterion；scikit-learn `FactorAnalysis` 官方文档仅作实现语义核对。
- Hotelling (1936), canonical correlations。
- Lee & Seung (2000), NMF 乘法更新与两类损失。
- Fisher (1936), 多组判别；Hastie, Tibshirani & Friedman,
  *The Elements of Statistical Learning* 第 4 章。
- scikit-learn LDA/QDA 官方用户指南仅作实现、收缩和参数语义核对。

