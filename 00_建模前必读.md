# 建模任务契约与人工工作流

本文件保留给人工阅读和复杂任务的工作流复核，不再承担机器题型路由。机器调用先执行
`AGENTS.md` 与 `00Q_永久质量核心.md`，再由 `routing-manifest.yaml` 和
`tools/route_knowledge.ps1` 返回当前阶段的最小文件集合。

## 1. 九问任务契约

没有回答完以下问题，不进入复杂建模：

1. **决策问题是什么？** 最终要支持什么判断或行动？
2. **输出是什么？** 一个数、排序、类别、策略、曲线、路径还是情景集合？
3. **评价标准是什么？** 准确、成本、收益、公平、风险、稳健还是可解释？
4. **研究边界是什么？** 时间、空间、对象、目标总体和不考虑因素是什么？
5. **变量是什么？** 决策、状态、控制、外生、参数和随机变量分别有哪些？
6. **约束是什么？** 资源、物理、逻辑、法规、时间、非负和边界约束有哪些？
7. **数据是什么？** 来源、口径、单位、频率、样本量、缺失、依赖和偏差如何？
8. **描述、预测、解释还是因果？** 是否真的需要干预效应或结构外推？
9. **怎样证明可信？** 使用何种基线、划分、回测、理论检查、敏感性或外部核验？

将答案写成：给定什么、求解什么、满足什么约束、用什么标准比较、结论适用于哪里。

## 2. 阶段式工作流

### 问题定义

- 重述题目交付项，建立“问题—输出—证据”映射；
- 标明系统边界、可控与不可控因素、反馈、延迟和随机扰动；
- 列符号与单位，区分已知事实、待核验事实和建模假设。

### 数据阶段

运行路由的 `data` 阶段，执行 `00D_数据阶段门禁.md`。在来源、口径、主键、时间顺序、
依赖结构和预测时可得性尚未确定前，不进入正式模型比较。

### 选模与验证设计阶段

运行 `model_selection` 与 `validation_design` 阶段，执行 `00E_选模与验证设计门禁.md`：

- 先固定任务目标、响应支持集、依赖结构和部署单位；
- 建立结构匹配的简单基线；
- 冻结外层验证单位、划分、主指标和测试隔离边界；
- 只保留 2—5 个有明确角色、可被证伪的候选。

### 实现阶段

需要编码或实验时进入 `implementation` 阶段。公式与代码逐项核对索引、单位、随机性、目标方向、
约束方向、停止条件和软件版本；先用手算小例、边界或不变量检查，再运行完整数据。

### 验证阶段

首次正式比较模型、解释结果或形成结论前进入 `validation_audit` 阶段，完整执行
`11_验证敏感性与不确定性.md`。至少报告基线比较、训练外证据、误差或残差、关键敏感性、
合理压力情景、不确定性及失效条件。

### 交付阶段

只有正式论文、报告、代码或竞赛提交才进入 `delivery` 阶段，按路由读取写作、实验和提交规范；
动态竞赛规则必须按当前届次和官方版本重新核验。

## 3. 基线与复杂度原则

基线不是装饰。预测可用均值、上一期、季节性朴素或简单回归；分类可用多数类或逻辑回归；
评价可用等权；优化可用现行策略或简单可行解；仿真可用解析近似或极端边界。

每增加一个模块都要回答：

- 它解决了基线的哪个明确缺陷？
- 新参数是否能被当前有效样本识别？
- 训练外收益是否抵偿解释、计算和维护成本？
- 是否引入泄漏、过拟合、不可验证假设或不可靠外推？

若无法回答，保留更简单模型或把复杂方法降为探索性对照。

## 4. 假设的最低写法

每条关键假设说明：为什么需要、在什么范围成立、违反后影响哪个结论、如何检查或做敏感性。
“不考虑其他因素”不是可审计假设。合理假设必须与已知数据和外部事实分开记录。

## 5. 最小交付标准

- 符号、单位、研究边界和任务目标；
- 数据来源、版本、质量、口径、划分与泄漏检查；
- 模型结构、关键假设、参数来源或估计方法；
- 简单基线、主模型、求解设置和复现信息；
- 训练外或外部验证、敏感性、压力测试和不确定性；
- 结论、适用范围、可执行建议、局限与失效条件；
- 代码输出、结果表和正文数字的一致性。

## 6. 立即回退信号

- 训练表现异常优于测试，或随机划分破坏时间、空间、主体和组结构；
- 特征使用未来、事后、测试标签或部署时不可得信息；
- 公式量纲、索引、正负号、边界或代码实现无法对齐；
- 参数数量接近或超过有效独立样本，或关键参数不可识别；
- 排名、方案或结论对轻微合理扰动发生无解释翻转；
- 优化解违反约束、常识或物理边界，启发式却被写成全局最优；
- 相关性被直接写成因果，样本内拟合被写成外部有效；
- 数据口径、外部阈值、竞赛规则或软件行为无法从可靠版本确认；
- 输出精度超过输入数据和不确定性所支持的有效数字；
- 为了“全面”加载无关专题或整个来源区，反而挤占当前任务的推理和验证。

一次任务结束后，可用 `建模复盘模板.md` 记录最终模型链、失败尝试、敏感参数、有效验证、
可复用代码及下一次的首选路线；任务特定数值不得无条件提升为通用知识。
