# 知识条目与证据维护规范

本文件用于维护知识库，不是每次建模的必读内容。目标是让以后新增或修正的知识能够被快速定位、
按条件调用并追溯到证据，而不是要求把所有旧文件一次性重写。

## 0. 单一事实源与路径规则

- `routing-manifest.yaml` 是题型、阶段、required/optional/deferred/forbidden 映射的唯一手工维护源。
  `00B_任务路由与最小读取集.md` 由脚本生成，README 和 AGENTS 只说明调用协议，不复制详细映射。
- 每个方法的完整定义只在一个主专题中维护；轻量选择入口只保存门禁和链接，不复制方法正文。
- `sources/来源与证据台账.md` 仍是当前来源身份的权威登记。未来结构化 registry 只有在 schema、生成器、
  一致性测试和迁移计划全部通过后才能取代它；试点数据不得与台账同时手工维护同一事实。
- 新增的可导航或机器依赖引用使用标准 Markdown 相对链接；命令示例、字段名和纯文件标签可使用行内代码。
  机器字段中的知识库路径统一相对库根目录；
  库外文件显式标为 `external-file:`，历史删除记录标为 `tombstone:`，不得伪装成缺失的库内链接。
- 统计加载成本使用脚本计算的字节数或实测 token，不手工维护 `estimated_lines`。
- 文档审阅日期不能代替高风险主张的版本核验；动态内容记录 `verified_at`、`review_due`、适用届次或
  软件版本。超过复核日期只表示需要复核，不自动证明主张为假。

## 1. 主专题的推荐结构

新增方法卡或大幅修订专题时，按下列顺序组织；不适用的小节可省略，但不得省略关键边界：

1. 适用范围与触发词；
2. 任务契约：输入、输出、目标、数据生成或决策场景；
3. 统一符号、单位和假设；
4. 模型定义与算法；
5. 参数估计、实现与软件版本；
6. 基线、验证、敏感性和不确定性；
7. 失败模式、反例和立即否决信号；
8. 最低交付清单；
9. 来源与版本边界。

基础索引可以较短，但必须指向完整方法卡。来源审计、论文学习记录和历史复盘不得伪装成通用方法。

## 2. 哪些结论必须逐条绑定证据

以下属于高风险知识条目，不能只在文末笼统列参考文献：

- 外部事实、竞赛规则、行业参数、数据口径和获奖身份；
- 关键公式、定理、收敛性、最优性、统计分布和适用条件；
- 软件默认值、API 行为、求解器保证和版本相关实现；
- 性能比较、经验阈值、推荐参数和“通常更好”等经验结论；
- 从获奖论文、博客、课程笔记或生成式文本提取的做法。

每条至少记录：来源 ID、支持的具体主张、页码/章节/稳定锚点、版本或访问日期、适用条件和核验状态。
来源 ID 在 `sources/来源与证据台账.md` 中唯一登记。

## 3. 证据等级绑定主张

证据等级评价“该来源能否支持当前主张”，不是给整份资料永久打分。同一官方结果页可对奖项身份是
A 级，但其中转载的学生论文内容仍只是 C 级案例；原始方法论文可支持其定义，不自动证明软件当前实现
或新数据上的性能。

建议状态只使用以下词：

| 状态 | 含义 | 是否可进入主专题 |
|---|---|---|
| 已核验 | 来源、定位、条件和必要复算均通过 | 可以，保留条件 |
| 条件通过 | 在明确版本、假设或范围内成立 | 可以，必须紧邻写条件 |
| 案例线索 | 只证明有人这样做过 | 只能进入案例或候选区 |
| 待核验 | 来源、版本或复算尚缺 | 不得写成事实 |
| 已隔离 | 已发现错误、冲突或不适用 | 只保留风险说明 |

不得使用“已学习”代替上述核验状态。

## 4. 推荐的主张记录格式

重要新增知识使用下表，或在正文紧邻位置给出等价信息：

| Claim ID | 主张 | 来源 ID 与定位 | 条件/版本 | 核验动作 | 状态 |
|---|---|---|---|---|---|
| 示例 | 不在此填真实结论 | `SOURCE-ID`，章节/页码 | 数据与软件版本 | 手算、代码或交叉来源 | 待核验 |

Claim ID 只需在当前方法卡内唯一；来源 ID 必须在全局台账唯一。若来源更新导致结论变化，保留旧版本
边界并在 `CHANGELOG.md` 记录影响范围。

## 5. 公式与代码的落库门槛

公式进入主专题前必须：

1. 给出符号、索引、集合和单位；
2. 写明独立、平稳、凸、可微、可识别等条件；
3. 检查量纲、正负号、归一化分母和目标方向；
4. 检查零、常数、空集、极端值或已知解析小例；
5. 若有实现，核对公式—代码的索引、单位、随机性和停止条件；
6. 记录软件、依赖、求解器和随机种子；
7. 不能复算时标“待核验”，不靠语言润色掩盖。

代码仓库热度和运行成功都不能证明数学正确。外部代码先做安全静态检查，再在隔离目录做最小例、
不变量、边界和多种子测试。

## 6. 文件角色与默认加载边界

| 文件角色 | 默认用途 | 是否默认进入建模上下文 |
|---|---|---|
| `AGENTS`、`00Q` | 触发协议与永久质量核心 | 正式建模常驻 |
| `routing-manifest.yaml`、`00B` | 唯一路由源与生成视图 | 脚本解析；不要求把整个清单塞入上下文 |
| `00D`、`00E`、`11` | 数据、选模/验证设计、验证审计 | 到达对应阶段时加载 |
| `00`、`00A`、`01` | 人工工作流、完整证据协议、问题识别 | 路由或维护时按需 |
| `02`—`19` | 基础专题 | 按题型 |
| `22`—`35A` | 进阶标准、严谨方法卡与完整索引 | 按具体模型；完整索引不默认 |
| `20`、`21`、复盘 | 案例与表达模式 | 仅需要案例对照时 |
| `30` | 特定网站的版本与风险映射 | 条件读取 |
| `sources/` | 原始证据、审读记录、候选材料 | 追溯时读取 |
| `CHANGELOG`、质量审计 | 维护状态 | 维护任务读取 |

原始 PDF、图片和代码不因存在于目录就自动成为可信知识，也不应整批进入上下文。

## 7. 更新流程

1. 新资料先进入 `sources/`，登记来源、权限、版本、哈希和证据角色；
2. 使用 `资料消化模板.md` 区分事实、公式、案例、推断和待核验项；
3. 用原始/官方来源与复算通过门禁；
4. 只把可复用且已核验的内容写入对应专题；
5. 若需改变调用映射，只更新 `routing-manifest.yaml`，不得同时手工改 `00B` 的路由表；
6. 更新必要的来源台账和 `CHANGELOG.md`；
7. 运行 `tools/validate_library.ps1`；若路由清单、路由脚本、生成视图或入口协议发生变化，再先运行
   `tools/generate_routing_docs.ps1` 与 `tools/test_routing.ps1`，并读取全部命令的完整输出；
8. 重要建模任务使用 `建模复盘模板.md` 回写成功与失败证据。

路由测试与知识正确性测试分开：路由测试只验证必读召回、禁止加载、阶段门禁和路径可达；公式、代码、
数据与结论仍按对应专题、证据门禁和任务级实验独立核验。没有通过黄金路由测试前，不切换入口或归档旧文档。

## 8. 遗留内容处理

现有早期基础专题有一部分只在中央台账或后续方法卡中给出来源，尚未逐条绑定 Claim ID。不得因此
声称已经达到逐公式可追溯。后续每次实际调用或修改相关专题时，优先补齐当次使用的关键公式和结论，
而不是为了形式一次性添加无法核实的引用。
