# 数学基础与统计理论补充

本专题用于连接“会套模型”和“理解模型为什么成立”。建模时不需要重证所有定理，但要知道结论依赖哪些条件，
以及条件破坏后会影响估计、优化还是数值求解。

## 1. 矩阵结构与建模意义

### 秩、子空间与可识别性

对设计矩阵 \(X\)：

- 列满秩意味着线性模型参数在给定结构下可唯一估计；
- 秩亏意味着存在精确线性依赖，参数不唯一；
- 接近秩亏表现为病态或强共线，参数虽然形式上唯一但对扰动敏感；
- 零空间描述不改变观测输出的参数方向；
- 列空间描述模型能够拟合的输出集合。

因此，共线问题不能只看相关系数；还要看奇异值、条件数和参数不确定性。

### 广义逆与最小二乘

Moore–Penrose 逆 \(A^+\) 可处理非方阵和秩亏矩阵：

\[
x^\*=A^+b
\]

当 \(Ax=b\) 无精确解时，它给出最小二乘解；当解不唯一时，它给出欧氏范数最小的解。

实际计算优先使用 QR、SVD 或求解器接口，不显式形成 \(A^+\) 或 \((X^\top X)^{-1}\)。

### 投影

满列秩时，线性回归拟合值是 \(y\) 在 \(X\) 列空间上的正交投影：

\[
\hat y=Hy,\qquad H=X(X^\top X)^{-1}X^\top
\]

\(H\) 为帽子矩阵，\(h_{ii}\) 衡量杠杆。高杠杆点不一定残差大，却可能强烈影响参数。

### 二次型与正定性

\[
q(x)=x^\top Ax
\]

若对称矩阵 \(A\succ0\)，二次型严格为正，常对应严格凸目标、协方差逆矩阵或能量。
半正定矩阵允许零方向，可能意味着非唯一解。

### 实数域与复数域不能混写

- 实正交矩阵满足 \(Q^\top Q=I\)；
- 复酉矩阵满足 \(Q^HQ=I\)；
- 实对称矩阵由正交矩阵对角化；
- 复 Hermitian 矩阵由酉矩阵对角化。

因此复 Hermitian 情形不能把 \(Q\) 称为“正交矩阵”，也不能用转置代替共轭转置。
特征向量应作为 \(Q\) 的列按同一约定排列；代码使用 `eigh` 等 Hermitian 专用算法，
并用 \(\|Q^HQ-I\|\)、\(\|AQ-Q\Lambda\|\) 验证。

## 2. 矩阵分解怎么选

| 分解 | 主要条件 | 建模用途 |
|---|---|---|
| LU（带主元） | 一般方阵 | 多次解线性方程、行列式 |
| Cholesky | 对称正定 | 协方差、最小二乘、快速稳定求解 |
| QR | 一般矩形矩阵 | 最小二乘，通常比正规方程稳定 |
| SVD | 任意矩阵 | 秩、条件数、伪逆、低秩近似、病态问题 |
| 特征分解 | 方阵；对称阵性质最好 | 稳定性、PCA、Markov、二次型 |

建模报告不必展开算法证明，但应在病态、大规模或精度敏感问题中说明使用何种分解。

## 3. 收敛方式与渐近理论

常见收敛：

- 几乎必然收敛；
- 依概率收敛；
- 依分布收敛；
- \(L^p\) 收敛。

通常强收敛能推出部分弱收敛，反向不自动成立。统计建模中最常用：

- **大数定律**：样本平均趋近总体期望，是频率估计和 Monte Carlo 的基础；
- **中心极限定理**：适当标准化的和近似正态，是标准误和大样本区间的基础；
- **Slutsky 定理**：把依概率收敛的估计量代入渐近分布；
- **Delta method**：把估计量的渐近正态传播到光滑函数。

若
\[
\sqrt n(\hat\theta-\theta)\overset d\longrightarrow N(0,\Sigma),
\]
且 \(g\) 可微，则
\[
\sqrt n[g(\hat\theta)-g(\theta)]
\overset d\longrightarrow
N\left(0,\nabla g(\theta)^\top\Sigma\nabla g(\theta)\right).
\]

小样本、重尾、强相关或边界参数下，大样本近似可能很差，应使用 bootstrap、精确方法或仿真校准。

## 4. 估计量的评价

区分：

- **估计目标 estimand**：真正要估计的总体量；
- **估计量 estimator**：样本的函数；
- **估计值 estimate**：代入本次样本得到的数值。

常见性质：

- 无偏：\(E(\hat\theta)=\theta\)；
- 一致：样本增加时 \(\hat\theta\) 收敛到 \(\theta\)；
- 方差与效率；
- 均方误差：

\[
MSE(\hat\theta)=Var(\hat\theta)+Bias(\hat\theta)^2
\]

无偏不等于预测最好。适当偏差可换取更低方差，这也是 Ridge、收缩估计和部分正则化方法的基础。

### 矩估计

令样本矩等于理论矩求参数。实现简单，但不一定最有效，且高阶矩对异常值敏感。

### 极大似然

\[
\hat\theta_{MLE}=\arg\max_\theta L(\theta;y)
\]

在正则条件下通常具有一致性、渐近正态和渐近有效性。若参数在边界、模型混合、样本很小或不可识别，
常规标准误和卡方近似可能失效。

### 充分性与 UMVUE

充分统计量保留样本关于参数的全部信息。完全充分统计量可帮助构造一致最小方差无偏估计。
竞赛建模不必机械追求 UMVUE，但要理解“无偏、方差最小”只是在指定估计类和损失下的性质。

## 5. 凸性与最优性

集合 \(C\) 凸，当任意 \(x,y\in C\) 与 \(\theta\in[0,1]\) 满足：

\[
\theta x+(1-\theta)y\in C
\]

函数 \(f\) 凸，当：

\[
f(\theta x+(1-\theta)y)
\le \theta f(x)+(1-\theta)f(y)
\]

可微凸函数满足一阶支撑条件：

\[
f(y)\ge f(x)+\nabla f(x)^\top(y-x)
\]

二阶可微时，Hessian 半正定是凸性的判据之一。

### 为什么凸性重要

- 局部最优即全局最优；
- 一阶条件通常足够；
- 对偶理论更完整；
- 算法和停止证据更可靠；
- 解对扰动的分析更清楚。

非凸问题必须更谨慎地表述“最优”，并用多初值、下界、松弛或全局算法提供证据。

### 线搜索与 Newton

下降算法的一般形式：

\[
x_{k+1}=x_k+\alpha_kd_k
\]

方向 \(d_k\) 和步长 \(\alpha_k\) 都影响收敛。固定全步 Newton 在最优点附近可能二次收敛，
但远离最优点、Hessian 非正定或初值差时可能失败。实际使用阻尼 Newton、线搜索或信赖域。

## 6. 多元统计补充

### PCA 的两个重要提醒

1. 用协方差矩阵还是相关矩阵取决于尺度含义；标准化消除量纲影响，也会把各变量方差统一为 1。
2. PCA 利用相关结构降维，但不能自动判断重复变量在业务上是否应删除。加入重复变量会改变总方差分配，
   因此指标构建阶段仍需先处理语义重复。

完整的 SVD 等价、秩边界、载荷定义、维数选择、训练外变换和 PCR 验证见
`27_主成分分析严谨方法卡.md`。

### 非负矩阵分解（NMF）

对非负矩阵：

\[
X\approx WH,\qquad W,H\ge0
\]

它产生“加性部件”表示，适合词频、像素、交互计数和基因表达等天然非负数据。

- 连续强度、近似高斯噪声：常用 Frobenius 平方误差；
- 计数/频数、近似 Poisson：常用广义 KL 散度；
- 分解不唯一，结果依赖秩、初始化和正则；
- 用重构误差、稳定性和下游任务共同选择秩。

完整维数、KL 边界、因子似然、CCA 白化 SVD 和判别分数见
`28_多元统计扩展严谨方法卡.md`。

### 典型相关分析（CCA）

CCA 在两组变量中分别寻找线性组合，使组合之间相关最大。适合研究“指标组 A 与指标组 B 的整体关联”。

注意：

- 需要足够样本与稳定协方差估计；
- 高维或共线时使用正则 CCA；
- 高典型相关仍是相关，不是因果；
- 用留出样本验证典型相关，防止过拟合。

### 判别分析

- 距离判别：依据类中心与距离；
- LDA/Fisher：寻找类间分离相对类内变化最大的方向；
- Bayesian 判别：结合类条件密度、先验与误分类成本。

先验类别概率和误分类成本会改变决策边界，应在报告中明确。
正式使用必须读取 `28_多元统计扩展严谨方法卡.md`。

## 7. 聚类方法补充

### K-modes

K-means 的均值和欧氏距离不适合纯类别数据。K-modes 使用类别众数作为原型、简单匹配不相似度作为距离。
结果仍依赖初始化和 \(K\)。

### OPTICS

DBSCAN 使用单一密度阈值，对不同密度簇不稳定。OPTICS 输出点的排序与可达距离，
可从可达距离图中观察多个密度尺度的聚类结构。

### 外部评价

有真实标签时可用 Rand Index、Adjusted Rand Index 等。ARI 校正随机一致性，通常比原始 RI 更适合比较。
无标签时仍需稳定性、轮廓系数、解释性和外部变量验证。

## 8. 时间序列理论补充

### 弱平稳

弱平稳要求均值恒定、协方差只依赖滞后。很多 ARMA 结论建立在弱平稳基础上。
平稳不是“图看起来平”，要结合结构、单位根与残差诊断。

### 差分与位移算子

位移算子 \(B y_t=y_{t-1}\)，一阶差分：

\[
\Delta y_t=(1-B)y_t
\]

季节差分：

\[
\Delta_s y_t=(1-B^s)y_t
\]

过度差分会增加噪声和不必要的 MA 结构。

### 因果性、可逆性与 Wold

- ARMA 的因果性保证当前值可由过去冲击表示；
- 可逆性保证冲击可由当前和过去观测恢复；
- Wold 分解说明纯非确定性弱平稳序列可表示为白噪声的无限移动平均。

Wold 是理论存在性结论，不等于有限阶 ARMA 一定正确；实际仍需选阶、诊断和回测。

## 9. EM 算法

含潜变量或缺失数据时，EM 迭代：

1. E 步：在当前参数下计算完整数据对数似然的条件期望；
2. M 步：最大化该期望，更新参数。

EM 使观测似然非下降，但可能收敛到局部最优或鞍点，且靠近最优点时可能较慢。
应使用多初值、记录似然轨迹、检查收敛与退化解。

典型应用：GMM、隐变量模型、部分缺失数据、隐马尔可夫模型。
完整定义、成立条件、ELBO—KL 恒等式、GMM 公式和收敛边界见
`33_EM算法与潜变量模型严谨方法卡.md`。

## 10. 何时必须回到理论条件

- 想把样本结果推广到总体；
- 要报告显著性、置信区间或标准误；
- 矩阵病态、参数不唯一；
- 优化器只找到局部解；
- 模型位于参数边界或存在混合成分；
- 时间序列非平稳；
- 高维样本量不足；
- 使用潜变量或缺失数据；
- 输出要进入高风险决策。

一句话原则：**公式给出计算，条件决定结论能否成立。**
