# 分类、聚类与机器学习

本文件给出基础路线；正式机器学习任务还必须读取 `24_机器学习评审标准.md`。
使用 CART、Bagging、随机森林、GBDT 或 XGBoost 时还必须读取
`31_树模型与集成学习严谨方法卡.md`。
使用感知机、MLP、RNN、LSTM、GRU 或 CNN 时还必须读取
`32_神经网络严谨方法卡.md`。

## 1. 监督学习流程

问题定义 → 数据划分 → 预处理管道 → 基线 → 特征 → 候选模型 → 交叉验证调参 →
阈值/校准 → 独立测试 → 解释与稳健性。

所有预处理、特征选择、重采样和调参必须在训练折内部完成。

## 2. 常见分类模型

### 逻辑回归

\[
P(y=1|x)=\frac{1}{1+\exp[-(\beta_0+\beta^\top x)]}
\]

优点：可解释、概率输出、良好基线。连续变量与 log-odds 的线性关系可用样条放松。
系数的指数是 odds ratio，不能直接当概率变化。
正式使用读取 `29_线性模型与广义线性模型严谨方法卡.md`。

### LDA/QDA

基于类条件分布。LDA 假设各类共享协方差，QDA 允许不同协方差但参数更多。

### 决策树与集成

- 单树：规则直观但不稳定；
- 随机森林：降低方差、稳健；
- 梯度提升：表格数据常强，需调深度、学习率和正则；
- 类别特征、缺失和不平衡要按实现妥善处理。

以上仅作索引。正式使用必须按 `31_树模型与集成学习严谨方法卡.md` 核对加权分裂、
bootstrap/OOB、随机特征子集、GBDT 的 raw-score 空间、XGBoost 正增益符号、软件版本和训练外验证。

### SVM

最大间隔分类；核函数处理非线性。对特征缩放和 \(C,\gamma\) 敏感；大样本训练成本高。

### KNN

简单、非参数；对尺度、维度、局部密度敏感。

### 神经网络

本节只作路由。神经网络必须按 `32_神经网络严谨方法卡.md` 核对输出—目标—损失契约、
反向传播归约、优化器状态、梯度稳定、序列 mask、GRU 版本差异、卷积/互相关语义和多种子验证。
小样本表格任务先与 Logistic、正则线性模型和树集成公平比较。

## 3. 评估与阈值

混淆矩阵产生：

- Precision \(=TP/(TP+FP)\)；
- Recall \(=TP/(TP+FN)\)；
- Specificity \(=TN/(TN+FP)\)；
- F1 为 Precision 与 Recall 的调和平均。

阈值应依据漏报/误报成本、资源限制或净收益选择，而非默认 0.5。

概率模型还应检查：

- Brier score；
- calibration curve；
- log loss；
- 分组校准。

ROC-AUC 衡量排序能力；类别极不平衡且关注正类时 PR 曲线通常更直接，但 PR-AUC 的基线随正例率变化，
跨数据集比较必须同时报告正例率。

## 4. 聚类

本节仅作模型索引。正式聚类任务必须继续读取 `26_聚类分析严谨方法卡.md`，并按其中的
距离审计、边界约定、参数敏感性、重采样稳定性和外部/下游验证协议执行。

### K-means

最小化簇内平方和。适合近似球状、尺度相近的数值簇。

要标准化、多个初值；用肘部、轮廓系数、Gap statistic 辅助选 \(K\)。

### 层次聚类

输出树状图，适合探索层级。链接方法（single/complete/average/Ward）会改变结果。

### DBSCAN

基于密度，可发现任意形状并识别噪声；对 \(\varepsilon\)、min_samples 和密度差异敏感。

### GMM

概率混合模型，给出软分类；可用 AIC/BIC 选择成分数。
完整责任度、E/M 更新、ELBO—KL、协方差塌缩、标签交换和停止条件见
`33_EM算法与潜变量模型严谨方法卡.md`。AIC/BIC 只是候选模型内的相对证据。

### K-modes 与 OPTICS

- K-modes 使用众数原型和匹配不相似度，适合纯类别数据；
- OPTICS 输出排序与可达距离，适合探索不同密度尺度，比单一 \(\varepsilon\) 的 DBSCAN 更灵活。

聚类没有标签时，内部指标不能证明“真实类别”。必须结合稳定性、可解释性和外部变量验证。
用于选择算法、特征、距离或簇数的外部变量已经参与调参，不能再作为最终独立验证证据。

## 5. 降维

- PCA：线性、最大方差；正式使用读取 `27_主成分分析严谨方法卡.md`；
- t-SNE：局部可视化，不适合解释全局距离或直接作为正式聚类证据；
- UMAP：保留局部结构并兼顾部分全局结构，仍受参数影响；
- Autoencoder：非线性、大数据，但解释弱。
- NMF：非负数据的加性低秩表示，适合词频、像素、计数和交互数据；
- CCA：寻找两组变量中相关最大的线性组合，适合两组指标整体关联分析。

二维嵌入图漂亮不等于数据天然分群。

PCA 不会自动解决指标语义重复；重复变量会改变方差分配。NMF 的损失函数应匹配噪声：
连续强度常用 Frobenius，计数数据可考虑广义 KL。详见 `19_数学基础与统计理论补充.md`。
因子分析、NMF、CCA、LDA/QDA 或 Fisher 判别的完整条件、公式与验证见
`28_多元统计扩展严谨方法卡.md`。
监督任务中插补、缩放和 PCA 必须在训练折内拟合；低方差方向仍可能包含预测信号。

## 6. 特征选择与重要性

- Filter：相关、互信息、统计检验；
- Wrapper：递归特征消除；
- Embedded：Lasso、树正则；
- Permutation importance：模型无关，但强相关特征会分摊重要性；
- SHAP：解释模型预测贡献，不代表因果效应。

特征选择必须在交叉验证内部完成。

## 7. 调参与模型选择

- 网格搜索：参数少；
- 随机搜索：高维更有效；
- 贝叶斯优化：单次训练昂贵；
- 嵌套交叉验证：外层评估，内层调参；
- Early stopping：控制迭代过拟合。

搜索次数越多，对验证集的隐性过拟合越强，因此保留独立测试集。

## 8. 小样本原则

- 优先简单模型与正则化；
- 减少特征维度；
- 用重复交叉验证或 bootstrap 报告不确定性；
- 不轻易使用深度模型；
- 不只报告最优折，应报告均值、标准差或置信区间；
- 学习曲线判断主要受偏差还是方差限制。

## 9. 常见错误

- 全数据标准化/筛特征后再交叉验证；
- 先 SMOTE 全数据再划分；
- 用准确率评价极不平衡问题；
- 用测试集反复调参；
- 把 SHAP 值写成因果贡献；
- 在时间、空间或个体相关数据上随机划分；
- 只保存模型，不保存特征顺序、预处理和类别映射。
