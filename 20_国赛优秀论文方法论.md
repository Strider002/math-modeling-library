# 国赛优秀论文方法论

本专题基于全国大学生数学建模竞赛组委会在中国大学生在线展示的 2022—2025 年代表论文。
官方展示页未逐篇标注奖级，因此这里统一称“官方展示论文”，不把无法核验的奖级写成事实。

2025 年官方展示入口已加入来源台账。B157 已由全国组委会正式获奖名单核验为本科组全国一等奖，
并完成 67 页逐页深审、公式独立核验与 FFT 初值复算；其余 2025 展示论文仍不得提前写入具体模型链：
https://dxs.moe.gov.cn/zx/hd/sxjm/sxjmlw/2025qgdxssxjmjslwzs/

学习目标不是复刻模型，而是提炼高质量竞赛论文中可迁移的：

- 问题拆解；
- 模型链设计；
- 数值求解；
- 验证方式；
- 摘要与正文表达；
- 容易被忽略的缺陷。

## 1. 十篇论文的模型链

### 2025 B157：碳化硅外延层厚度的干涉反演

模型链：

1. 用 Snell 定律、薄膜一次往返相位和 Fresnel 系数建立双光束基线；
2. 通过滑窗 FFT、双角度频率一致性和 SNR 选择工作波段；
3. 由 FFT 主频给出厚度初值；
4. 联合 10°/15° 光谱反演共享厚度、色散参数和校准量；
5. 以分段色散模型处理高波数段结构误差；
6. 对硅光谱用 Airy 类模型讨论多次反射；
7. 用残差、子波段、敏感性和合成数据做内部诊断。

可复用做法：

- 采用“机理基线 → 频域初值 → 多观测联合反演 → 模型升级 → 诊断”的逆问题链；
- 让不同角度共享同一物理厚度，而把角度/波数校准量作为受约束的扰动；
- 先用低成本 FFT 定位周期和初始化，再用非线性前向模型精化；
- 在一般单层膜复振幅框架内比较双光束与多光束，不把模型名称当结论。

独立复算：

- 官方附件上复现 SiC 10°/15° 的 FFT 初值 `7.661989/7.682250 μm`，对应论文 `7.662/7.682 μm`；
- 复现 Si 10°/15° 的 FFT 初值 `3.675063/3.680777 μm`，对应论文 `3.675/3.681 μm`；
- 这只确认固定波段、固定有效折射率下的 FFT 初始化，不确认后续 Cauchy/Airy 解唯一或精度正确。

必须保留的警示：

- 薄膜传播相位使用 `4πnd cos(θ_t)/λ₀`，界面相位另计；几何路径的 `1/cos(θ_t)` 不能直接替代同波前相位；
- Cauchy 系数须绑定晶型、晶向、偏振、温度、掺杂、波长单位和有效波段，不能跨样品外推；
- 全谱拟合报告 `B=-0.100`，而截图代码下界也是 `-0.10`；参数触边时应检查多初值、边界扰动、Jacobian/SVD 和 profile likelihood；
- 论文把 RMSE `1.35×10⁻³ → 9.34×10⁻⁴` 写成“提升约 14.4 倍”，正确是原/新约 `1.445`，或 RMSE 下降约 `30.8%`；
- Q—Q 图的 `R²` 不是正式正态性检验；未报告 Ljung—Box 的滞后、统计量和 p 值时，不能断言残差为白噪声；
- 同一光谱上的波段选择、拟合、子带稳定性和残差诊断属于内部验证，不替代独立样品/独立测量；
- 相干长度不是硬阈值；Airy 简式需说明复折射率、偏振、仪器线形、粗糙度、角度展宽和平行度条件；
- 截图代码无可执行源、依赖锁和提交哈希，故 `7.619/7.604/3.814 μm` 等非线性结果不作为已复现真值。

完整证据、代码和边界见 `sources/国赛/2025_CUMCM_B157/审读记录.md`。

### 2022 A001：波浪能装置输出功率优化

模型链：

1. 根据牛顿第二定律建立浮子—振子垂荡耦合动力学；
2. 用四阶 Runge–Kutta 求状态轨迹；
3. 以阻尼器平均输出功率为目标；
4. 用梯形求积近似功率积分；
5. 用遗传算法搜索阻尼参数；
6. 扩展到垂荡与纵摇耦合；
7. 做模型检验与误差分析。

可复用做法：

- 先完成物理状态方程，再讨论优化；
- 将瞬时功率通过时间积分转为平均功率；
- 数值积分、状态求解与参数优化各自分层；
- 对不同阻尼函数形式分别建模，而不是用一个黑箱统一拟合。

提醒：

- 元启发式需要与网格、小规模精确解或多初值比较；
- 必须检查数值步长和积分误差；
- 最优功率要说明是在稳定周期、有限窗口还是稳态下定义。

### 2022 B030：无人机编队纯方位无源定位

模型链：

1. 用方位角与相对位置建立几何定位模型；
2. 对不同已知信息情形分类讨论；
3. 用遍历/枚举识别可定位组合；
4. 生成仿真样本验证最少发射机数量；
5. 将定位模型嵌入多轮编队调整；
6. 用迭代几何关系更新角度和距离。

可复用做法：

- 先回答“是否可识别”，再回答“如何估计”；
- 对信息条件不同的情形分支建模；
- 把定位和控制拆成两个层次；
- 用合成真值数据验证逆问题。

提醒：

- 仿真验证必须覆盖噪声、退化几何和异常方位；
- “理想仿真可定位”不等于现实测量下稳定；
- 应报告位置误差随角度误差和构型变化的敏感性。

### 2022 C155：古代玻璃制品成分分析与鉴别

模型链：

1. 删除无效数据并对成分比例作中心化对数比变换；
2. 用卡方/Yates 修正分析类别关联；
3. 用描述统计和回归估计风化前成分；
4. 用决策树完成分类；
5. 用 Q 型聚类细分亚类；
6. 对未知样本分类；
7. 用灰色关联分析成分之间的关系。

可复用做法：

- 识别数据是“成分数据”，比例和为常数，不能直接按普通欧氏数据处理；
- 数据清洗、关联、预测、分类、聚类按任务分层；
- 分类结果同时报告 Accuracy、Recall、Precision、F1；
- 对分类边界做扰动敏感性。

提醒：

- 多个分类指标同时为 1 是强烈的泄漏或过拟合检查信号；
- 小样本下树模型和聚类结果需要重采样稳定性；
- 灰色关联度不能直接解释成化学因果关系；
- 风化前成分属于反事实估计，应报告不确定区间。

### 2023 A0165：定日镜场优化设计

模型链：

1. 将太阳光入射、镜面调节、阴影遮挡、集热器截断分解；
2. 建立光线方向、镜面法向和效率计算；
3. 对镜场网格化；
4. 计算光学效率、余弦效率、阴影遮挡效率、截断效率与输出；
5. 用黄金分割搜索部分连续变量；
6. 用三分查找或蒙特卡洛搜索镜宽、镜高、数量、位置、高度等；
7. 检验并评价模型。

可复用做法：

- 将总效率拆为有物理含义的乘积或模块；
- 先做固定结构求性能，再做结构参数优化；
- 通过网格化把复杂连续场简化为可计算单元；
- 摘要给出完整最优设计参数与单位。

提醒：

- 多阶段单变量搜索可能忽略变量交互；
- 需要验证网格分辨率、光线采样量与遮挡计算误差；
- 蒙特卡洛给出的“最优”应报告重复结果与置信程度。

### 2023 B477：多波束测深合理探测方案

模型链：

1. 用二维平面几何求覆盖宽度和重叠率；
2. 推广到三维倾斜测线；
3. 用解析几何计算不同位置覆盖；
4. 以重叠率为约束，用贪心迭代确定测线；
5. 对真实海域先插值、分区和随机森林拟合地形；
6. 改进启发式路径算法，搜索覆盖路线；
7. 统计覆盖率、路线长度与漏测区域。

可复用做法：

- 从二维解析模型逐步推广到三维与真实地形；
- 先用解析关系缩小搜索空间，再用启发式求复杂路线；
- 最终结果同时给覆盖率、路线长度和超重叠长度；
- 地形建模服务于路线优化，子模型输入输出清楚。

提醒：

- 插值、分区和随机森林三者的选择要有验证对照；
- 路线优化应给简单扫描线或贪心基线；
- 覆盖率很高时仍要检查漏测位置是否集中于关键区域；
- 启发式算法应报告多随机种子和最优值分布。

### 2023 C228：蔬菜商品自动定价与补货决策

模型链：

1. 数据筛选、整合、异常和缺失处理；
2. 用 ACF、统计分布、偏相关和 FP-Growth 分析销售规律；
3. 用 LSTM 预测未来成本与销量；
4. 用对数需求模型估计价格弹性；
5. 用 VIKOR 筛选候选单品；
6. 用 NSGA-II 优化收益和市场需求；
7. 输出补货与定价方案；
8. 提出新增数据建议。

可复用做法：

- 形成“描述 → 预测 → 决策”闭环；
- 类别层级和单品层级分别建模；
- 将价格弹性直接嵌入定价决策；
- 多目标优化呈现利润与需求之间的权衡。

提醒：

- 模型名称过多时容易形成“模型堆砌”；
- LSTM 需要时间回测，不能只报告训练拟合或 \(R^2\)；
- 预测误差应传递进补货优化；
- 关联规则和偏相关不能直接当作替代/互补的因果证据；
- VIKOR 筛选阈值、NSGA-II 参数和 Pareto 选点需做敏感性。

### 2024 A163：板凳龙运动路径

模型链：

1. 用等距螺线和极坐标描述龙头路径；
2. 建立相邻把手位置递推；
3. 建立速度递推；
4. 用板凳几何关键点构造碰撞判别；
5. 在盘出阶段用阿基米德螺线与 S 形转向连接；
6. 用二分法搜索调头空间或最大允许速度；
7. 反复代入检验位置、速度和碰撞。

可复用做法：

- 使用局部几何递推避免一次性求解几百节构件；
- 把“路径、位置、速度、碰撞、转向”拆成清楚的模块；
- 通过单调性使用二分法，而不是盲目调用复杂优化器；
- 关键判别函数直接对应现实碰撞条件。

提醒：

- 递推误差可能沿长链累积；
- 碰撞判别必须说明矩形宽度、端点和非相邻构件；
- 二分前要证明或数值验证单调性与可行区间；
- 需要用极端时刻和可视化逐帧核验。

### 2024 B159：生产过程中的决策优化

模型链：

1. 对供应商次品率做单侧假设检验；
2. 用二项分布和几何分布描述抽检；
3. 建立利润期望最大化的检测/拆解决策；
4. 用状态—决策模型描述多阶段生产；
5. 用动态规划和记忆化求最优策略；
6. 用贝叶斯估计更新次品率；
7. 将后验分布代入决策，分析参数不确定性。

可复用做法：

- 统计检验回答“是否接收”，优化模型回答“怎样生产”；
- 状态定义、转移和决策边界清晰；
- 将参数估计的不确定性传入后续决策；
- 用贝叶斯后验重新决策，而不是只替换一个点估计。

提醒：

- 假设检验的第一类/第二类错误成本应与抽检方案一致；
- 期望利润最大不代表尾部风险可接受；
- 动态规划必须检查状态是否包含足够历史信息；
- 先验与后验预测需要敏感性分析。

### 2024 C038：农作物种植策略优化

模型链：

1. 数据可视化和亩均利润分析；
2. 构建七年利润最大化模型；
3. 加入地块、面积、轮作、豆类、重茬、产销等约束；
4. 用差分进化改进遗传算法求解不同滞销情景；
5. 引入销售量、亩产量、成本和价格不确定性；
6. 用 CVaR 兼顾收益与风险；
7. 加入替代/互补和变量相关结构；
8. 用鲁棒优化与动态调参做敏感性。

可复用做法：

- 把农业规则翻译为明确约束；
- 点情景、随机情景、相关情景逐层扩展；
- 用 CVaR 避免只优化平均利润；
- 用热力图和年度利润曲线展示时空方案。

提醒：

- 若模型本质可写为 MILP，应先用精确求解器建立基准；
- 相关系数不应直接等同于替代/互补机制；
- CVaR 的置信水平和场景生成方式必须解释；
- 启发式收敛曲线不是全局最优证明；
- 多年方案应检查所有年度和地块约束，而不只看总利润。

## 2. 跨论文的共同结构

## 2.1 子问题不是并列，而是递进

常见递进方式：

1. 第一问建立基本状态、几何、统计关系或确定性模型；
2. 第二问增加参数估计、分类、约束或连续决策；
3. 第三问推广到多维、动态、实际数据或联合模型；
4. 最后一问加入不确定性、优化、策略或一般化。

写作时应画出子问题输入输出：

```text
基础数据/机理
    ↓
参数或状态估计
    ↓
预测/分类/覆盖计算
    ↓
优化决策
    ↓
敏感性与建议
```

若后问完全换模型而不复用前问结果，通常说明问题分析还不够统一。

## 2.2 摘要采用“逐问闭环”

高质量摘要的稳定句式：

1. 总述目标和总思路；
2. “对于问题一”：核心假设/结构 → 方法 → 关键数值结果；
3. “对于问题二”：在前问基础上的新增因素 → 方法 → 结果；
4. 依次回答所有问题；
5. 最后一句说明检验、稳健性或局限；
6. 关键词只保留最核心的对象、模型和算法。

摘要不能只列模型名。至少应出现：

- 研究对象；
- 模型关系；
- 核心算法；
- 关键结果；
- 一项可信性证据。

## 2.3 模型链比模型数量重要

优秀论文常混合多个工具，但每个工具有明确角色：

- 微分方程产生状态；
- 数值积分求轨迹；
- 优化搜索参数；
- 仿真检验可识别性；
- 统计模型估计不确定输入；
- 风险模型控制尾部；
- 可视化核验中间机制。

判断一个方法是否应保留，只问：

> 它解决了模型链中的哪个必要问题？如果删掉，结论会失去什么？

答不出来的模型通常是装饰。

## 2.4 A/B/C 题的典型重心

不是硬规则，但近年样本呈现：

- **A 题**：物理机理、几何、运动学、连续变量优化；
- **B 题**：几何与运筹、定位、流程决策、离散状态；
- **C 题**：数据清洗、统计/机器学习、预测和经营优化。

选题时应匹配团队能力：推导、代码、数据、优化和写作哪个更强。

## 3. 验证方式要与模型类型匹配

| 模型类型 | 最有说服力的验证 |
|---|---|
| 物理/机理 | 守恒、量纲、极限、解析简例、步长收敛 |
| 几何/定位 | 合成真值、退化构型、测量噪声、可视化 |
| 分类/预测 | 时间/分层留出、混淆矩阵、校准、分组误差 |
| 聚类 | 多初始化、重采样稳定性、外部变量解释 |
| 优化 | 可行性、基线、下界/gap、小规模精确解 |
| 元启发式 | 多随机种子、收敛分布、参数敏感性、精确基准 |
| 风险/鲁棒 | 场景覆盖、尾部表现、压力测试、失效概率 |
| 多阶段决策 | 状态充分性、转移核验、策略仿真 |

官方展示论文中常见“收敛曲线、扰动分析、仿真验证、误差分析”，但后续建模不应满足于形式存在，
而要检查验证是否真正针对最危险的假设。

## 4. 图表的高价值用途

图表优先用于：

- 展示几何结构和变量关系；
- 显示清洗前后数据；
- 展示模型链流程；
- 检查状态轨迹和碰撞；
- 显示空间/时间决策；
- 比较基线和主模型；
- 展示 Pareto、风险和敏感性；
- 暴露异常与失败情景。

低价值图表：

- 与正文数字完全重复的柱状图；
- 无单位、无图例的“结果图”；
- 只有优化器收敛曲线却没有基线；
- 只展示成功案例，不展示误差或边界。

## 5. 可模仿的写作动作

1. 每问开头先用一段话解释难点和选择模型的理由；
2. 先给变量、参数和单位，再给公式；
3. 复杂模型先给总结构，再展开子模块；
4. 每个公式后说明现实含义；
5. 求解算法写输入、输出、初始化、停止条件；
6. 关键结论同时给数值、单位和适用条件；
7. 每问末尾做小结并把输出交给下一问；
8. 最终结论使用表格集中回答题目要求。

## 6. 不应照搬的模式

- 为每一小问换一个流行模型；
- 用“结果很好”代替基线和外部验证；
- 分类指标全为 1 却不调查泄漏；
- 用相关性直接定义因果、替代或互补；
- 用 \(R^2\) 代替时间序列回测；
- 用元启发式收敛曲线宣称全局最优；
- 预测给优化只传点估计；
- 参数、阈值和权重取值无依据；
- 在摘要堆模型名而不回答最终数值；
- 把优秀论文的错误或薄弱环节当成“获奖标准”。

## 7. 建模时的国赛论文对照清单

### 问题分析

- [ ] 子问题是递进关系
- [ ] 每个子模型有明确输入输出
- [ ] 主线能用一张图说明
- [ ] 模型选择来自题意而非熟悉度

### 模型与求解

- [ ] 先有基线
- [ ] 变量、单位和约束完整
- [ ] 解析关系优先于黑箱
- [ ] 元启发式有精确/简单对照
- [ ] 预测误差进入后续决策

### 验证

- [ ] 验证方式匹配模型类型
- [ ] 最危险假设有敏感性
- [ ] 报告失败或退化情景
- [ ] 结论精度不超过数据精度

### 表达

- [ ] 摘要逐问给方法和数值
- [ ] 图表服务于证据
- [ ] 每问末尾有清楚答案
- [ ] 最终建议包含触发条件和局限

## 7.1 从竞赛论文走向赛题后续研究

CUMCM 2026 年赛题后续研究申请通告给出的官方研究报告框架是：

1. 综述相应赛题的优秀论文；
2. 分析已有方案的不足；
3. 提出具有创新性的模型或算法；
4. 与已经发表的工作比较并说明优长；
5. 尽量结合企业或应用部门的实际需求。

这套框架适合重要论文的二次审读和赛后深化：不能只摘“用了什么模型”，还要建立基线、指出缺口、
给出可复核改进并验证实际价值。它来自赛题后续研究项目的申请与专家评审要求
（`CUMCM-FOLLOWUP-2026`），不是竞赛论文量化评分表，不能用来倒推某篇论文的奖级或具体得分。

## 8. 最核心的经验

高质量国赛论文的优势通常不是某个“高级算法”，而是：

1. 把复杂现实拆成可计算模块；
2. 让子问题共享同一套变量与逻辑；
3. 用合适的数值方法把模型真正算出来；
4. 用与题型匹配的证据说明结果可信；
5. 在摘要中把方法和结论一次讲清。

一句话总结：

**先让模型链闭环，再追求算法亮点；先给可信证据，再追求漂亮结果。**
