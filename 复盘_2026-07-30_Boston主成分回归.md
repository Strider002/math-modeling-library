# Boston Housing 主成分回归复盘

## 任务信息

- 题目/项目：Boston Housing 数据集主成分回归
- 日期：2026-07-30
- 数据范围：506个历史人口普查分区，13个自变量，响应变量为 `MEDV`
- 最终交付：Quarto HTML、QMD 源文件和独立 Python 源码

## 问题与模型链

- 任务类型：监督回归、PCA 降维、训练外模型比较
- 最终模型链：数据审计 → 固定训练/测试集 → 折内标准化 → PCA → 线性回归 → 重复交叉验证选维 → 测试集评价 → bootstrap 与敏感性分析
- 子模型之间的输入输出：PCA 将13维标准化自变量映射为得分，线性回归使用前 \(k\) 个得分预测 `MEDV`
- 选择理由：变量尺度不同且存在相关结构；PCR 可比较“压缩维数”与“保留预测信号”的权衡

## 最终结果

- 关键数值：
  - 训练集 CV RMSE 严格最低点：\(k=13\)
  - 一标准误差规则：\(k=6\)，累计解释方差85.9%
  - 6维 PCR 测试集：RMSE 5.444、MAE 3.308、\(R^2=0.596\)
  - OLS 测试 RMSE 4.929；Ridge 测试 RMSE 4.954
- 相对基线提升：PCR 明显优于均值基线，但未优于 OLS 或 Ridge
- 不确定区间：固定测试集5,000次成对 bootstrap 中，PCR 相对 OLS、Ridge 的 RMSE 差值区间均高于0
- 最终建议：PCR 用作6维结构压缩；若目标是本次设定下的线性预测，优先 OLS 或 Ridge

## 验证证据

- 数据外验证：20%固定测试集；无新时期或空间分块数据
- 残差/误差：高价端明显低估，残差重尾并偏离正态
- 敏感性：比较相关矩阵 PCR、协方差 PCR、删除 `B` 后的相关矩阵 PCR
- 稳健性：训练集10折5次重复交叉验证；测试集成对 bootstrap
- 极端情景：测试集中 `MEDV=50` 的3个样本 RMSE 为18.602，明显高于其他样本的4.478

## 有效做法

1. 把标准化、PCA 和回归放在同一管道内，避免全数据 PCA 泄漏。
2. 用监督式训练外误差选 \(k\)，不把85%累计贡献率当作唯一规则。
3. 同时报告严格最优 \(k=13\) 与一标准误差简化解 \(k=6\)，明确预测与压缩目标的冲突。

## 失败尝试

| 尝试 | 失败表现 | 原因 | 何时可能有效 |
|---|---|---|---|
| 将 `.csv` 按逗号和首行表头读取 | 得到505行、1列字符串 | 文件实际是无表头的空白分隔格式 | 真正逗号分隔且带表头的数据 |
| 只按累计解释率确定 PCR 维数 | 无法证明保留预测信号 | PCA 不使用响应变量 | 仅以重构或压缩为目标时可作候选规则 |
| 用6维 PCR 期待自动改善预测 | 测试误差高于 OLS/Ridge | 后续低方差方向仍含预测信息 | 高维、强病态且降维能明显降低估计方差时 |

## 最敏感与最危险的假设

- 参数/输入：PCA 是否标准化、保留主成分数、历史变量 `B`
- 假设：随机划分近似代表同分布新分区，且空间相关可以忽略
- 失效后影响：测试误差可能偏乐观，模型无法推广到新时期、其他城市或空间外样本

## 可复用资产

- 代码：折内 PCR 管道、重复 CV 选维、一标准误差规则、测试集成对 bootstrap
- 数据处理：无表头空白分隔 Boston 数据读取与哈希核验
- 图表：解释方差—CV RMSE 双图、相关载荷热图、预测与残差四联图
- 文字：PCR“压缩有效但预测未胜出”的审慎结论
- 参数：随机种子42，80%/20%划分，10折5次重复 CV，5,000次 bootstrap

## 下次首选路线

1. 若有空间坐标，先做空间分块验证而不是随机划分。
2. 同时比较 PLS、Ridge 与非线性模型，并保留最终独立测试。
3. 对截顶目标采用适合上界或删失机制的模型，不让少数高价样本被整体平均误差掩盖。

## 应更新的知识库文件

- `27_主成分分析严谨方法卡.md` 已覆盖 PCR 监督选维、泄漏和尺度敏感性，本次无需修改原专题。
