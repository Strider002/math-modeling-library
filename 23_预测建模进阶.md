# 预测建模进阶

适用于时间序列、面板、计数、概率和“预测后优化”任务。预测的核心证据是模拟真实使用时点的训练外回测，不是样本内 \(R^2\)。

## 1. 预测任务契约

开始前固定：

- 目标 \(y\)、单位、聚合频率；
- 预测时刻 \(t\) 能看到的信息集 \(\mathcal F_t\)；
- 预测步长 \(h\) 和更新频率；
- 点、分位数、区间或完整分布；
- 评估对象：逐时点、逐主体、总体还是尾部；
- 错误代价是否非对称；
- 目标数据是否会事后修订。

同一模型在 \(h=1\) 和 \(h=12\) 的优劣可能相反。必须按步长报告。

## 2. 基线

至少包含与数据结构匹配的基线：

\[
\hat y_{t+h|t}=y_t\quad\text{（朴素）},
\qquad
\hat y_{t+h|t}=y_{t+h-m(k+1)}\quad\text{（季节朴素）},
\]

其中 \(m\) 为季节周期，\(k=\lfloor(h-1)/m\rfloor\)。另可加入历史均值、漂移法、简单线性趋势和业务现行规则。

复杂模型不能稳定优于合理基线时，不得用“模型更先进”替代性能证据。

## 3. 时间回测

滚动预测起点：

1. 只用起点之前数据拟合全部预处理和模型；
2. 预测固定步长集合；
3. 起点前移并重复；
4. 按步长、时期和主体汇总误差；
5. 最后保留未参与选择的一段作测试。

滑动窗口用于旧数据失效，扩展窗口用于历史仍有信息。窗口长度本身是超参数，只能在训练/验证阶段选择。

面板数据需同时防止时间泄漏和主体泄漏；若目标是预测新主体，测试主体必须整体未见。

## 4. 特征可用性

对每个特征记录：

| 特征 | 事件时间 | 数据产生时间 | 可查询时间 | 修订延迟 |
|---|---|---|---|---|

只要可查询时间晚于预测时点，就不能作为当时特征。滚动均值必须使用 `shift` 后的历史，目标编码和聚合统计只在训练折计算。

外生变量未来值未知时，必须：

- 同时预测外生变量；
- 使用已知计划值；
- 或构造有依据的情景。

用真实未来外生值回测会高估可部署性能。

## 5. 误差指标

\[
MAE=\frac1n\sum_t|e_t|,\qquad
RMSE=\sqrt{\frac1n\sum_te_t^2}.
\]

MASE 使用训练样本的朴素误差缩放。非季节情形：

\[
MASE=\frac{\frac1J\sum_{j=1}^{J}|e_j|}
{\frac1{T-1}\sum_{t=2}^{T}|y_t-y_{t-1}|}.
\]

季节序列将分母改为
\[
\frac1{T-m}\sum_{t=m+1}^{T}|y_t-y_{t-m}|.
\]

分母为零时 MASE 未定义。MAPE 在 \(y_t=0\) 时未定义、近零时爆炸，也会产生不对称惩罚；不要用随意加 \(\epsilon\) 后仍称标准 MAPE。

多序列汇总要说明宏平均、按样本加权还是按业务量加权。指标选择必须对应决策损失。

## 6. 概率预测

区间预测同时报告覆盖率和宽度。只追求覆盖率会得到无用的宽区间。

分位数 \(q_\tau(x)\) 的 pinball loss：

\[
L_\tau(y,q)=
\begin{cases}
\tau(y-q),&y\ge q,\\
(1-\tau)(q-y),&y<q.
\end{cases}
\]

概率预测还可用对数评分、Brier score、CRPS。选择阈值前先校准概率；测试集不能参与校准器或阈值选择。

Conformal prediction 的覆盖保证依赖交换性或相应时序扩展条件。普通 split conformal 不应无条件用于强非平稳序列。

## 7. 模型族选择

### 统计时序

- ETS：水平、趋势、季节的状态空间表达；
- ARIMA/SARIMA：差分后线性自相关结构；
- 动态回归/状态空间：外生变量和时变状态；
- 结构时间序列：趋势、季节、回归项分解。

检查残差白噪声、参数稳定性、根条件和回测表现。单位根检验不能代替结构判断。

### 表格机器学习

先构造合法滞后特征，再用正则回归、树集成、SVR 等。树模型不能自然外推超出训练响应的趋势；外推任务要显式趋势或机理结构。

### 深度时序

仅在样本量、序列数量和复杂依赖足够时考虑。必须与季节朴素、ETS/ARIMA、梯度提升等强基线比较，并进行多种子、消融和计算成本报告。

## 8. 特殊数据

### 计数

计数非负且常异方差。考虑 Poisson、负二项、零膨胀或合适的计数损失；先检验过度离散和零机制。用普通高斯回归时检查负预测。

### 间歇需求

大量零加偶发正值时，普通 MAPE 失效。考虑 Croston 系列、发生—规模两阶段模型或概率模型，并用时间回测与库存损失评价。

### 层级序列

总量与分项预测应满足加总一致性。可分别预测后做 reconciliation；评估各层级，不能只看总量。

### 结构突变

断点前后机制不同。可用分段、时变参数、变点或滑动窗口。不得随机混合突变前后数据来声称泛化。

## 9. 模型比较

报告每个起点、步长的配对误差。均值差异外，还应给：

- 中位数与分位数；
- bootstrap 或适合依赖结构的区间；
- 不同时期/主体的胜率；
- 极端期表现；
- 复杂度和运行成本。

Diebold–Mariano 检验需要损失差序列和相应依赖处理；显著性不替代实际改善幅度。

## 10. 预测进入优化

点预测优化通常低估风险。至少采用一种：

- 高/中/低场景；
- 从预测分布抽样的随机规划；
- 分位数或 chance constraints；
- 不确定集合的鲁棒优化；
- 滚动时域再优化。

必须评估“决策质量”：成本、收益、可行率、regret、尾部风险，而不只是预测误差。预测误差更小不保证决策更好。

## 11. 最低交付

- 预测契约与信息截止时间；
- 至少两个合理基线；
- 回测起点、窗口和步长；
- 所有预处理在折内拟合；
- 整体和分组/分步长误差；
- 残差、漂移和极端时期；
- 点预测之外的区间/情景；
- 模型选择与最终测试分离；
- 若进入优化，报告决策后果。

## 12. 评委否决信号

- 随机 K 折验证普通时间序列；
- \(R^2\) 很高就称预测准确；
- 滞后/滚动特征包含当前或未来目标；
- 用测试集反复调参；
- 只与很弱基线比较；
- LSTM 样本很少且无多种子/消融；
- 外生变量使用了回测时真实未来值；
- 只给点预测，不说明预测期越远不确定性增大。

## 13. 主要来源

- Hyndman & Athanasopoulos, *Forecasting: Principles and Practice*, 3rd ed.：
  https://otexts.com/fpp3/
- Rolling forecasting origin：
  https://otexts.com/fpp3/tscv.html
- 具体模型公式正式使用时回查对应教材、包文档和版本。

