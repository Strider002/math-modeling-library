# C 题 NIPT 检测策略优化：全题终稿复盘与可复用方法卡

> **条目类型：** 任务级终稿复盘，不是临床指南或通用模型定理。  
> **状态：** 条件通过。数值仅对应本题附件、最终代码和本文列出的决策设定；可复用的是问题拆解、验证设计、失败边界与代码审计规则。  
> **终稿优先级：** 本文覆盖并更新 `复盘_2026-08-10_C题问题2区间删失AFT与机会约束分组.md` 与 `复盘_2026-08-10_C题问题3多因素区间删失与稳健分组方案.md` 中的草案性结论。两份旧文档仍保留为历史试验记录，不得引用其过时的两组方案或“问题三尚未实现”的表述。

## 1. 任务档案与证据边界

| 项目 | 记录 |
|---|---|
| 题目 | C 题：基于 NIPT 数据的 Y 染色体达标时点优化与女胎异常筛查 |
| 最终报告 | `D:\数学建模\2025模拟\SvpohSGacdffe718bcaa3b6e835c03ae3461cab1\C题\朱秀锐，李文杰，王明轩.pdf`，34 页，SHA-256：`B098B92575D3044F896BFE5E8E821AF7D512F467829E0FF7736E9360A2EF63A2` |
| 原始附件 | `C题\附件.xlsx`，已登记哈希：`14827156218BD4F7E4F16DB4AA6D9F757C6648379E038AE6C6B58383648614AF` |
| 男胎数据 | 1082 条原始记录、267 名孕妇；细粒度抽血批次键合并后为 1063 个批次 |
| 女胎数据 | 605 条记录、147 名孕妇 |
| 最终运行原则 | 所有交叉验证、Bootstrap 和抽样传播均按“孕妇代码”分组；禁止按单条记录随机划分 |
| 结果证据 | 终稿 PDF、终版 notebook 和 `paper/paper/outputs/tables` 中的自动输出；没有外部独立队列，因此所有性能均为内部验证证据 |

本题最重要的边界是：NIPT 是**筛查**而非确诊；模型输出只能是给定样本、阈值和风险偏好下的群体决策或复检建议，不能写成临床诊断、个体保证或因果结论。

### 1.1 问题链的正确拆分

| 问题 | 真正要解决的对象 | 主输出 | 不应混淆为 |
|---|---|---|---|
| 问题一 | 连续 Y 浓度与孕周、BMI 的重复测量条件关联 | 关系方程、效应方向、显著性和诊断 | 对新孕妇的高精度预测 |
| 问题二 | 首次达到 4% 的未知时间 | BMI 连续区间与最早可靠检测时点 | 首次“被观测到达标”的普通回归 |
| 问题三 | 新增事前因素是否有稳定增量，以及误差下的稳健时点 | 多因素增量裁决、保守分组与时点 | 为了与问题二不同而强加复杂模型 |
| 问题四 | 稀少异常标签下的筛查分流 | 正常—复检—筛查异常三态 | 临床确诊或强行输出三个亚型诊断 |

这四问形成“**关系解释 → 达标时间决策 → 多因素增量与稳健修正 → 异常筛查分流**”的链条。它们共享数据审计和孕妇级验证，但不能共用不相同的统计口径：问题一使用同期动态 BMI，问题二、三使用统一的孕 12 周基准 BMI。

## 2. 全题最有价值的数据与建模技巧

### 2.1 先定义分析单位，再计算任何统计量

本题至少有三个数据粒度，混用会直接改变结论：

1. **原始测序记录：** 用于保留技术重复和女胎分类标签；
2. **抽血批次：** 同一实际抽血的技术重复需要聚合；
3. **孕妇：** 交叉验证、Bootstrap、达标时间和 BMI 分组的独立单位。

问题一最初按

\[
K_1=(\text{孕妇代码},\text{检测抽血次数})
\]

聚合，得到 1021 个观测；问题二、三改用更细的

\[
K_2=(\text{孕妇代码},\text{检测日期},\text{检测抽血次数}),
\]

得到 1063 个抽血批次。检查发现只用 \(K_1\) 会使 39 个分组跨不同检测日期。这个发现说明：**业务主键不能只凭字段名猜测，必须检查同一键是否跨时间和是否真是同一次抽血。**

**可复用规则：**

- 原始记录、技术批次和个体各自的样本量必须同时报告；
- 任意 join 或聚合之后都检查行数、唯一孕妇数和跨日期冲突数；
- 训练/验证、重抽样和不确定性区间按最高依赖层级（这里是孕妇）进行；
- 已发现问题一的聚合键比问题二、三粗，后续若重做第一问，应以细粒度批次键复算并比较结论，而不是静默假定两种口径等价。

### 2.2 动态 BMI 与基准 BMI 必须按任务分工

同一孕妇的“首次 BMI”对应的孕周不一致，直接拿首次 BMI 分组会把测量时点差异误当成体型差异。为此，终稿没有采用“首次 BMI”，而是建立统一的孕 12 周基准 BMI。

设 \(W_{ij}\) 为孕周、\(B_{ij}\) 为当期 BMI。用孕妇内中心化关系估计平均变化斜率：

\[
B_{ij}-\bar B_i=\gamma(W_{ij}-\bar W_i)+\varepsilon_{ij},
\qquad \hat\gamma=0.110676.
\]

- 若有 \([11,14)\) 周观测，直接选最接近 12 周的 BMI；
- 否则选距 12 周最近的观测，再按 \(\hat\gamma\) 线性校正；
- 260 名孕妇直接取窗内观测，7 名窗外校正；窗外者必须带来源标记并进入敏感性分析。

这不是“临床上 12 周必然最优”的外部事实，而是为本题**统一分组口径、避免未来信息泄漏**作出的建模约定。问题一研究同步关系，应使用 \(B_{ij}\)；问题二、三在首次安排检测前需要一个固定分组量，应使用 \(BMI_{i,12}\)。

### 2.3 事件没被精确观察到时，不能伪造精确事件时间

设 \(T_i\) 为第 \(i\) 名孕妇 Y 浓度首次达到 4% 的真实孕周。检测只在离散孕周进行，因此 \(T_i\) 不是已知点，而是：

\[
(L_i,R_i]=
\begin{cases}
(0,R_i], & \text{首次检测已达标（左删失）},\\
(L_i,R_i], & \text{相邻两次检测之间达标（区间删失）},\\
(L_i,+\infty), & \text{最后一次仍未达标（右删失）}.
\end{cases}
\]

本题 267 名男胎孕妇中，左删失 217 人、区间删失 43 人、右删失 7 人，左删失比例很高。若把“第一次被观测到达标的孕周”当作精确事件时间：

- 首检已达标者会被系统性推迟；
- 右删失者会被丢失；
- 检测日期非随机时，普通回归的偏差无法用漂亮的拟合图掩盖。

此外有 33 人（12.36%）首次达标后又低于 4%。因此“首次跨过 4%”只是本题统一的事件定义，并不等于后续稳定达标，最终建议必须保留复检与动态调整机制。

### 2.4 不确定性必须穿过“预测—分组—优化”整条链

技术重复批次估计出 Y 浓度误差标准差

\[
s_e=0.004711.
\]

终稿使用 100 次孕妇级 Bootstrap、100 次测量误差 Monte Carlo 和 500 次联合传播。每一轮不是只重算一个系数，而是重新执行：

```text
孕妇重抽样 / Y 浓度扰动 / 孕周扰动
        ↓
重新判定 4% 达标状态并构造删失区间
        ↓
重新拟合 AFT（及问题三的候选扩展）
        ↓
重新选择 BMI 组数、切点和组内最早可行时点
        ↓
汇总方案的入选频率、时点区间和约束满足情况
```

700 次全部成功这一事实只说明当前实现没有发生拟合失败；它不等于 700 是任何任务的万能次数。以后应同时报告随机种子、失败次数、分位数 Monte Carlo 误差和增加模拟次数后结论是否稳定。

## 3. 各问最终模型、收获与边界

### 3.1 问题一：重复测量下的 Y 浓度关系模型

#### 最终模型链

1. 将 Y 浓度作 Logit 变换 \(z_{it}=\log\{y_{it}/(1-y_{it})\}\)，边界 0 或 1 仅为计算截断到 \([10^{-6},1-10^{-6}]\)；
2. 用每名孕妇总权重为 1 的加权多元线性回归作总体平均基线；
3. 用随机截距混合模型处理同一孕妇的重复检测；
4. 比较线性、孕周平方和“孕周×BMI”结构，以 AIC、BIC、最大似然比检验和残差诊断选择主模型。

最终主模型为

\[
z_{it}=\beta_0+\beta_1g^c_{it}+\beta_2(g^c_{it})^2+\beta_3b^c_{it}+u_i+\varepsilon_{it},
\]

其中 \(u_i\sim N(0,\sigma_u^2)\)，\(\varepsilon_{it}\sim N(0,\sigma^2)\)。中心化使截距和一次项的解释落在样本平均孕周、平均 BMI 附近。

#### 最终结果与可说的结论

| 项目 | 结果 | 正确表述 |
|---|---:|---|
| 孕周一次项 | \(0.0399\)，\(P<0.001\) | 给定 BMI 后，Y 浓度的 Logit 尺度与孕周正向关联 |
| 孕周平方项 | \(0.002584\)，\(P<0.001\) | 上升曲线存在轻微向上弯曲，不能说成“生物机制被证明” |
| BMI 项 | \(-0.0229\)，\(P=0.0038\) | 控制孕周后 BMI 与 Y 浓度负向条件关联 |
| 重复结构 | \(ICC\approx0.732\) | 同一孕妇记录高度相关，不能把 1021 条记录当作独立样本 |
| 结构选择 | 平方项模型 AIC=810.34，线性模型 AIC=828.15 | 二次项在当前参数模型下有稳定的拟合增量 |

#### 做得好的地方

- 没有把重复测量当普通横截面数据；
- 同时保留了简单加权回归基线和混合效应主模型；
- 报告了 Wald、似然比、AIC/BIC 与残差诊断，显著性不只靠一个 P 值；
- 明确区分了固定效应总体曲线与含随机截距的“已有孕妇条件拟合”。

#### 必须保留的局限

- 条件拟合 \(R^2\approx0.80\) 使用了该孕妇已有记录的随机截距，**不能**写成“对新孕妇预测准确率 80%”；
- 残差有尾部偏离和局部异方差，当前正态同方差随机截距设定只是可用近似；
- 问题一的粗批次键与问题二、三细批次键不完全一致，细粒度复算是最优先改进项；
- 一度用 Ridge 样条做交叉验证而主模型是 MixedLM 的做法不成立：两者的随机效应、样条基和估计目标不同，不能用前者的泛化改善为后者背书；
- 将 \(Y\ge4\%\) 做二分类 GEE 属于问题二的阈值事件任务，只能作为附加敏感性，不应挤占问题一连续浓度关系与显著性主线。

### 3.2 问题二：区间删失 AFT + 有序动态规划的时点决策

#### 达标概率模型

以标准化后的 \(BMI_{12}\) 为解释变量，比较 Weibull、对数正态和对数 Logistic AFT：

\[
\log T_i=\beta_0+\beta_1x_i+\sigma\epsilon_i.
\]

以 Weibull 为例，

\[
F_i(t)=P(T_i\le t\mid BMI_{i,12})
=1-\exp\left\{-\exp\left[\frac{\log t-(\beta_0+\beta_1x_i)}{\sigma}\right]\right\}.
\]

删失似然必须分别使用 \(F(R_i)\)、\(F(R_i)-F(L_i)\)、\(1-F(L_i)\)。三种分布的 5 折孕妇级验证 NLL 几乎相同，终稿按“落在一标准误范围内优先简洁、候选顺序固定”的规则采用 Weibull，而不是把 0.00002 的差异包装成真实优越性。

估计结果表明，\(BMI_{12}\) 每增加 1 个单位，首次达标时间比约为 \(1.0366\)，即在当前样本与模型条件下达标时间约延长 3.66%。它是统计关联，不是 BMI 的因果效应。

#### 分组与时点不是聚类问题

对任何连续 BMI 区间 \(G_g\)，定义组平均达标概率

\[
p_g(t)=\frac{1}{|G_g|}\sum_{i\in G_g}F_i(t),
\]

并约束

\[
p_g(t)\ge0.90,\qquad Q_{0.10}\{F_i(t):i\in G_g\}\ge0.80.
\]

优化风险为

\[
R_g(t)=[1-p_g(t)]+\frac{t-10}{15}+\frac{[1-p_g(t)]^2}{15},
\]

在 10--25 周按日搜索最早可行时点。再用动态规划在 BMI 排序后的连续区间上求解

\[
DP(k,j)=\min_i\{DP(k-1,i)+C(i+1,j)\},
\]

限制候选组数 2--5、每组至少 25 人、相同 BMI 不拆分。这里使用动态规划而不是 K-means 或 WHO 固定 BMI 分类的理由是：**题目目标是让分组直接服务于时点风险，且需要连续、可执行的 BMI 区间。**

#### 最终方案与反思

| \(BMI_{12}\) 区间 | 人数 | 推荐时点 | 组平均达标率 | 低 10% 达标率 |
|---|---:|---|---:|---:|
| \(<32.45\) | 168 | 14 周 5 天 | 0.9000 | 0.8772 |
| \([32.45,34.84)\) | 63 | 16 周 4 天 | 0.9000 | 0.8894 |
| \(\ge34.84\) | 36 | 19 周 1 天 | 0.9001 | 0.8774 |

联合传播下 2、3、4、5 组的入选频率分别为 39.0%、50.2%、10.4%、0.4%。因此三组是“稳定性与可操作性较好的主方案”，不是数学上唯一绝对最优。高 BMI 组样本仅 36 人，其时点 95% 分位区间更宽，建议应带复检机制。

**最关键的经验：** 90% 平均可靠性、80% 低尾约束、延迟代价和两周复检间隔均是决策设定，不是数据自动估计出来的临床标准。阈值由 85% 提高到 95% 时，应重新联合优化切点与时点；不能仅把既有方案整体平移。

### 3.3 问题三：多因素“增量裁决”比强行升级模型更重要

#### 变量角色审计

附件的 31 个字段不应全部入模。事前可得的候选变量为年龄、身高、\(BMI_{12}\)、孕 12 周体重、怀孕/生产次数；孕周是时间轴。测序质量变量用于质控和测量误差，Y 浓度、Z 值、非整倍体结果等检测后指标是结果或未来信息，禁止进入初次检测时点模型。

体型变量满足

\[
BMI=\frac{Weight}{Height^2},
\]

故比较两条互斥路线：

- 路径 A：\(BMI_{12}+Age+Height\)；
- 路径 B：\(Age+Height+Weight_{12}\)。

孕产史可以扩展；辅助生殖仅 4 人，只能作敏感性变量，不能估计稳定主效应。

#### 候选模型与透明裁决

| 模型 | 验证 NLL | 裁决 |
|---|---:|---|
| \(M_0:BMI_{12}\) | 0.69204 | 采用为 AFT 基线 |
| \(M_1:BMI_{12}+Age+Height\) | 0.70151 | 无稳定增量 |
| \(M_2:Age+Height+Weight_{12}\) | 0.70038 | 无稳定增量 |
| \(M_3:M_1+\)孕产史 | 0.71127 | 无稳定增量 |
| 森林 \(RF_1\)：加入年龄、身高、孕产史 | OOF 表面改善 | 配对孕妇级 Bootstrap 的 LogLoss 区间跨 0，不采纳为决策模型 |

随机森林用于检查 AFT 的对数线性和分布假设是否遗漏非线性。它不能自然处理高度左删失的首次达标时间，且未通过预设的稳定增量判据。这个“未采纳”是高质量结果：它证明当前样本不足以支持复杂多因素主模型，而不是模型失败。

#### 为什么问题三仍然不同于问题二

如果多因素没有稳定预测增量，问题三仍需回答“误差、模型结构不确定性和达标比例如何影响决策”。终稿保持问题二的 Weibull AFT、BMI 主变量、风险函数和动态规划不变，只收紧概率口径：

\[
p_i(t)=\min\{F_{M_0}(t\mid BMI_{i,12}),F_{M_3}(t\mid x_i)\},
\]

并将“孕妇重抽 + Y 浓度误差 + 孕周误差”的联合传播结果取 10% 分位数后再进入同一优化器。最终得到：

| \(BMI_{12}\) 区间 | 人数 | 推荐时点 | 保守平均达标率 |
|---|---:|---|---:|
| \(<31.84\) | 148 | 17 周 0 天 | 0.9000 |
| \([31.84,35.11)\) | 88 | 19 周 1 天 | 0.9001 |
| \(\ge35.11\) | 31 | 23 周 1 天 | 0.9001 |

这不是“多因素使推荐时间后移”的证据。正确解释是：**多因素未证实有稳定增量；问题三较晚的时点来自更保守的概率口径。** 同一保守口径若施于问题二也会推迟时点。

#### 最重要的缺点与改进

- “双模型逐点取小”再取联合传播 10% 分位具有工程上的保守含义，但没有严格、唯一的概率学置信度解释，不能写成“90% 临床保证”；
- 23 周 1 天已接近可行动窗口上界，高 BMI 组仅 31 人，必须标为保守情景下的群体点估计，而非固定临床界值；
- 建议将 5%、10%、20% 分位及 85%/90%/95% 可靠性要求并列做敏感性表，展示风险—可靠性的权衡；
- 问题二、三的对比表必须明确“点概率口径”和“保守概率口径”，否则读者会误以为新增变量造成了效果提升。

### 3.4 问题四：从“分类模型”转为“筛查分流系统”

#### 最终模型链

1. 女胎 605 条记录、147 名孕妇，先以不使用异常标签的测序质量规则进行质控；
2. 低质量记录直接进入复检；可靠记录建立 T13、T18、T21 和独立 ANY 四个二分类终点；
3. 在外层孕妇分组的 5 折严格验证内，内层完成 Elastic-Net Logistic 调参、浅层 XGBoost、概率融合、Platt 校准与阈值选择；随机森林只作基线；
4. ANY 模型控制最终处置，采用低/高双阈值输出“正常—复检—筛查异常”；亚型只有在能力门控通过时才补充分型。

Elastic-Net Logistic 的加权目标为

\[
\min_{\beta_0,\beta}
-\sum_i\omega_{y_i}[y_i\log p_i+(1-y_i)\log(1-p_i)]
+\lambda\left[\alpha\|\beta\|_1+\frac{1-\alpha}{2}\|\beta\|_2^2\right].
\]

融合概率经 Platt 校准：

\[
\hat p_i=\operatorname{logit}^{-1}\{a+b\operatorname{logit}(0.6p_i^{LR}+0.4p_i^{XGB})\}.
\]

#### 最终输出与正确解释

在 347 条可靠记录上，ANY 的严格外层 OOF PR-AUC 为 0.4283、召回率为 0.7857；三态输出为正常 175 条、复检 126 条、筛查异常 46 条，自动判定覆盖率 63.69%。T13、T18、T21 亚型未全部通过自动分型的能力门控，因此终稿不应对亚型作独立自动诊断。

**分类题的可复用经验：**

- 稀少阳性时，准确率很容易虚高，应优先 PR-AUC、召回率、特异度、精确率、F1、Brier 分数和校准曲线；
- 同一孕妇的多条记录必须整体进入同一外层折；
- 阈值选择、校准、特征处理均必须在训练折内；
- 与其强迫所有样本二选一，不如把低置信样本显式送入复检；
- “ANY 异常”门控与“T13/T18/T21 亚型”分型应分层，不能把多个弱亚型概率简单取逻辑或覆盖总门控。

**仍未补齐的关键对照：** 题面和实际临床语义都使 \(|Z|>3\) 之类的单 Z 值规则成为必要基线。最终系统尚未在同一孕妇级嵌套验证下与该规则公平比较，因此不能声称多特征融合“已证明优于常规规则”。此外，质控阈值虽不使用标签，却借用了全体无标签特征分布，整个流水线不能称完全外层无信息借用；下一版应把 QC 规则固定为预先定义的阈值，或在每个外层训练折内重新估计。

## 4. 回归、树模型与校准：本题形成的选型卡

| 方法 | 在本题中的角色 | 真正解决的问题 | 不可越界的解释 | 最低验证 |
|---|---|---|---|---|
| 个体平衡加权线性回归 | 问题一基线 | 防止检测次数多的孕妇主导总体平均趋势 | 不能处理组内相关，也不是因果模型 | 与混合模型同尺度比较 |
| 随机截距混合模型 | 问题一主模型 | 用 \(u_i\) 表示孕妇稳定但未观测的差异，修正重复测量相关 | 条件 \(R^2\) 不等于新孕妇预测能力 | AIC/BIC、LRT、ICC、残差、影响诊断 |
| 区间删失 Weibull AFT | 问题二、三主时间模型 | 处理左、区间、右删失的首次达标时间 | 时间比是条件统计关联；分布不能称唯一真实 | 分布对照、孕妇级 NLL、边界概率检查 |
| 有序动态规划 | BMI 分组求解器 | 让连续 BMI 区间、最小组样本量、可靠性约束和风险目标一起决定切点 | 只是在给定候选组数、日网格和目标函数下最优 | 小规模穷举核验、约束违反量、重采样稳定性 |
| 校准随机森林 | 问题三非线性增量检验 | 给年龄、身高、孕产史一次非线性机会 | 记录级表面指标不能替代删失时间概率；重要性不等于因果 | GroupKFold、折内校准、多种子、配对孕妇 Bootstrap |
| Elastic-Net Logistic | 问题四可解释分类主干 | 稀少标签下兼顾稳定性与特征筛选 | 系数/优势比不等于概率变化或临床因果 | 嵌套 GroupKFold、校准、PR-AUC、分离检查 |
| 浅层 XGBoost | 问题四非线性补充 | 发现 Logistic 难以表达的交互或阈值 | 不能仅因样本内高分采用 | 严格嵌套验证、基线比较、校准后再用阈值 |
| Platt 校准与双阈值 | 问题四决策层 | 将模型概率转为正常/复检/筛查异常 | 只能保证当前内部分折下的校准改善 | Brier、可靠性图、阈值折间波动、外部验证 |

## 5. 这次明确淘汰或降级的做法

| 做法 | 为什么不作为主线 | 以后何时才考虑 |
|---|---|---|
| 把 1082 条记录当独立样本 | 同一孕妇重复测量使标准误和泛化证据失真 | 只有一人一条记录、或依赖结构已被正确建模时 |
| 用首次观测达标孕周做普通回归 | 左删失 217 人、右删失 7 人会造成系统偏差 | 只能作描述性基线 |
| 用首次 BMI 做问题二、三分组 | 首次测量孕周不一致，混入时间差异 | 若首次测量窗口被严格统一且均在决策时点前 |
| BMI、身高、体重同时进普通线性 AFT | 三者存在确定关系，解释和数值稳定性都坏 | 改用有物理含义的重参数化并验证可识别性 |
| WHO 固定 BMI 分类或 K-means 直接决定时点 | 不直接优化本题的达标、延迟和复检风险 | 可作为描述性对照，而不是主决策方法 |
| 用 Ridge 样条交叉验证替主 MixedLM 背书 | 两个模型的随机效应、样条基和估计目标不同 | 只有主模型本身在同一外层验证中被比较时 |
| 把随机森林直接替代 AFT | 难处理高比例左删失与真实首次达标时间 | 有更密集、更接近随机的观测，且有完整时间生存学习设计时 |
| 为使问题三不同而强行采用多因素模型 | AFT 与森林增量证据都不稳定 | 新样本量更大、早孕期观察更多、高 BMI 段更充足时重新检验 |
| 以普通准确率评价问题四 | 极不平衡标签下可以很高却漏掉阳性 | 几乎均衡分类、且业务代价对称时 |
| 直接输出亚型诊断 | T13/T18/T21 阳性太少、能力门控未通过 | 外部队列与更充分阳性样本支持后 |

## 6. 对最终论文的优点、弱点与赛场表达建议

### 6.1 优点

1. **结构与题意贴合。** 问题二、三没有把“检测到 4%”误作连续回归问题，而是转化为删失事件时间和风险决策。
2. **数据依赖被正视。** 重复测量、技术重复和同一孕妇多记录都进入模型、验证或不确定性设计。
3. **复杂模型有裁决机制。** 随机森林、XGBoost 等不是因为“高级”而上主线，而是接受基线、分组验证和稳定性门控。
4. **不确定性进入决策。** 不是只报告 AFT 参数区间，而是重跑“删失构造—拟合—分组—时点”全链。
5. **结论有边界。** Q1 不写因果，Q3 不把未稳定的多因素效果强称发现，Q4 保持筛查与复检定位。

### 6.2 主要不足

1. **问题一批次口径尚未完全统一。** 细粒度抽血批次键应回流复算问题一。
2. **问题三保守口径偏工程化。** `min(两模型概率)` 与联合传播 10% 分位叠加没有标准、唯一的置信概率解释，需以敏感性曲线替代“绝对可靠”语言。
3. **高 BMI 信息不足。** Q2 的高 BMI 组 36 人、Q3 的高 BMI 组 31 人，切点与时点区间都较宽，外推最危险。
4. **问题四的外部效度最弱。** 可靠记录仅 347 条、亚型阳性少、QC 使用全体无标签分布、缺少 \(|Z|>3\) 基线和独立外部批次。
5. **风险函数的临床权重非数据估计。** 90%/80% 阈值、延迟代价、复检间隔反映建模偏好，必须单列为“情景设定”。

### 6.3 最安全、最有说服力的终稿措辞

- 用“在本题数据与模型设定下”“条件关联”“群体层建议”“内部验证”替代“证明”“临床最优”“准确诊断”。
- 问题二写“3 组是重抽样下频率最高的可执行主方案”，不要写“唯一最优分组”。
- 问题三写“新增事前因素未表现出稳定增量；稳健方案因概率口径更保守而后移”，不要写“多因素导致时点后移”。
- 问题四写“筛查异常/复检建议”，不要写“判定患病”。
- 对任何效率、预测或风险改善，都紧邻给出比较对象、划分方式和数值指标。

## 7. 最终检查清单与下次首选路线

### 7.1 下次遇到同类题的最短闭环

1. 审计“记录—批次—个体”三层主键与缺失、冲突、重复；
2. 先画变量可用性时间线，删除任何决策时点后才能得到的变量；
3. 针对重复测量选混合模型/聚类标准误，针对模糊事件时间先判断删失类型；
4. 建立简单可解释基线，再增加非线性或机器学习候选；
5. 把候选模型的选择、校准、阈值和分组全部放进训练折或重抽样循环；
6. 若预测用于优化，传播到最终决策而非停在模型参数；
7. 输出“主方案 + 备选情景 + 不确定区间 + 不适用条件”，而不是一个孤立日期或阈值。

### 7.2 本题下一版的优先改进顺序

1. 以细粒度 \(K_2\) 批次键复算问题一，并报告方向、P 值和 ICC 是否变化；
2. 为问题三制作 5%/10%/20% 概率分位与 85%/90%/95% 可靠性组合表，明确鲁棒性—及时性权衡；
3. 扩充早孕、高 BMI 和区间删失样本，才能真正检验多因素模型的价值；
4. 将问题四 QC 固定为外部预定义规则或折内学习，并与单 \(|Z|>3\) 规则做严格的孕妇级嵌套对照；
5. 获得独立批次或外部队列，验证 Q2/Q3 时点和 Q4 概率校准；
6. 所有终稿数字直接由脚本导出，保存数据哈希、随机种子、依赖、失败运行数、代码版本和图表源数据。

## 8. 可检索关键词

`重复测量`、`随机截距混合模型`、`Logit 变换`、`个体平衡加权`、`首次达标`、`左删失`、`区间删失`、`右删失`、`Weibull AFT`、`机会约束`、`有序动态规划`、`BMI 基准化`、`孕妇级 GroupKFold`、`孕妇级 Bootstrap`、`测量误差 Monte Carlo`、`预测—优化不确定性传播`、`多因素增量裁决`、`共线性`、`校准随机森林`、`Elastic-Net Logistic`、`XGBoost`、`Platt 校准`、`PR-AUC`、`三态筛查分流`、`避免泄漏`、`临床筛查边界`。

## 9. 回写门禁

- [x] 任务级数值与通用方法边界已经分开；
- [x] 历史草案与终稿结论的优先级已明确；
- [x] 失败尝试、不可说的结论和适用条件一并保留；
- [x] 已登记终稿 PDF 的路径、页数与哈希；
- [ ] 若将 AFT、混合模型或筛查阈值的定义写入通用主专题，仍须补充原始方法论文/官方软件文档来源，不能只引用本任务结果；
- [x] 已在全部维护文件落盘后运行 `tools/validate_library.ps1`：`WARNINGS=0`、`ERRORS=0`、`AUDIT_STATUS=PASS`。
