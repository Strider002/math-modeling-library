# Iris 两份聚类报告对比审计复盘

## 任务信息

- 题目/项目：比较 `iris.html` 与 `iris聚类分析.html` 的数据、计算、选模逻辑和结论强度
- 日期：2026-07-28
- 数据范围：150 个 Iris 样本、4 个 cm 测量特征、3 个各 50 样本的外部物种标签
- 最终交付：对比评价与修正建议；不改动两份原始 HTML

## 问题与模型链

- 任务类型：聚类报告质量审计
- 最终模型链：来源与版本核验 → 数据一致性核验 → 指标独立重算 → 代码—正文一致性审计 → 尺度敏感性 → 结论强度评价
- 子模型之间的输入输出：两份报告均以同一 150×4 修订数据为输入，在 Z-score 特征空间比较 K-means、Ward 和 DBSCAN
- 选择理由：分别核验数据正确性、计算正确性、模型选择逻辑、稳定性与外部效度，避免把“数值算对”等同于“结论严谨”

## 最终结果

- 关键数值：
  - 两份报告的标准化 K-means 指标均可复现：K=2 时 WCSS 222.3617、CH 251.3493、轮廓系数 0.5818；K=3 时分别为 139.8205、241.9044、0.4599。
  - 第一份最终选择 K=3，但 CH 与轮廓系数均支持 K=2，选择依赖未量化的“细分程度”偏好。
  - 第二份将 K=2 作为内部结构主方案，并把 K=3 明确作为三物种外部参照，逻辑更一致。
  - 原始厘米尺度下，K-means 的 CH 在 K=3 更高（561.6278 对 513.9245）；Z-score 后则在 K=2 更高（251.3493 对 241.9044），说明主结论对尺度设定敏感。
  - 标准化与原始尺度的 K=3 划分 ARI 为 0.8036；不是可以忽略的完全一致。
- 相对基线提升：不适用；本任务比较报告质量。
- 不确定区间：两份报告均未给出 bootstrap/子采样稳定性区间或独立新样本验证。
- 最终建议：以第二份为主版本，但补充数据版本、原始尺度敏感性、DBSCAN 参数二维敏感性和重采样稳定性后再作为严谨终稿。

## 验证证据

- 数据外验证：无独立新样本；物种标签只构成同一数据上的外部一致性参照。
- 残差/误差：聚类任务不适用；关键表格由 scikit-learn 1.5.1 独立重算。
- 敏感性：原始尺度与 Z-score 会改变 CH 对 K 的选择；第二份只比较了 RobustScaler，未覆盖“不缩放”这一关键对照。
- 稳健性：第二份报告 20 个随机种子下 K-means 结果一致，并给出 RobustScaler 对照；两份均缺少重采样成员稳定性。
- 极端情景：第一份 DBSCAN 选择落在 20% 噪声上限附近（18.67%）；第二份落在 90% 覆盖率下限附近（90.67%），说明内部指标优化会利用允许排除样本的边界。

## 有效做法

1. 将数据值正确、计算可复现与选模逻辑严谨分开评价。
2. 对 DBSCAN 同时核查非噪声内部指标和覆盖率，禁止与全样本方法直接排名。
3. 用原始尺度对照揭示“标准化”本身是建模假设，而不是中性机械步骤。

## 失败尝试

| 尝试 | 失败表现 | 原因 | 何时可能有效 |
|---|---|---|---|
| 根据同目录旧 notebook 推断 `iris.html` 内容 | notebook 仅含 K-means，而目标 HTML 实际包含三种聚类方法 | HTML 不是该 notebook 的直接导出 | 已有明确源文件映射或哈希时 |

## 最敏感与最危险的假设

- 参数/输入：尺度方案、K、DBSCAN 的 eps、MinPts 与最低覆盖率
- 假设：Z-score 后四个变量应获得相同方差权重；允许把一定比例的低密度点作为噪声
- 失效后影响：推荐簇数、成员边界、外部 ARI 和 DBSCAN 的表观内部指标都会改变

## 可复用资产

- 代码：数据哈希/逐值比对、K=2—10 指标重算、原始/Z-score/RobustScaler 划分 ARI 对照
- 数据处理：明确区分 UCI `iris.data` 与修订的 `bezdekIris.data`
- 图表：建议增加“尺度 × K”指标表与“eps × MinPts × 覆盖率”稳定性图
- 文字：“计算正确不等于选模结论唯一；聚类结论仅在指定表示、距离和参数下成立。”
- 参数：审计环境 scikit-learn 1.5.1，K-means++、n_init=50、random_state=42

## 下次首选路线

1. 先逐值核验数据版本并保存哈希，再复算报告关键表格。
2. 分开评价内部结构、随机/重采样稳定性和外部一致性。
3. 对尺度、距离和噪声口径做最小实验矩阵，不用单一内部指标宣布唯一最佳方案。

## 应更新的知识库文件

- `26_聚类分析严谨方法卡.md` 已覆盖相关原则，本次只新增任务复盘。
