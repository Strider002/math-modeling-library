# 优化模型

本文件给出基础路线；正式优化任务还必须读取 `25_优化建模进阶.md`。

## 1. 数学规划的四要素

1. **集合与索引**：对象、时间、地点、资源；
2. **参数**：已知常量；
3. **决策变量**：模型真正选择的量；
4. **目标与约束**：评价选择并定义可行域。

写模型时先列符号表和单位，检查每个约束两边量纲一致。

## 2. 线性规划（LP）

标准形式：
\[
\min c^\top x,\quad Ax\le b,\quad x\ge0
\]

适用于目标和约束均线性、变量连续。重要概念：

- 可行域与最优解；
- 对偶变量/影子价格；
- 约束松弛量；
- 灵敏度范围；
- 不可行与无界诊断。

影子价格只在允许变化范围内作局部解释。

## 3. 整数与混合整数规划（IP/MILP）

用于：

- 是否选择：\(x\in\{0,1\}\)；
- 数量必须整数；
- 固定成本；
- 逻辑关系；
- 调度、路径、选址。

常见逻辑：

- 若选择才允许生产：\(q\le Mx\)；
- 至少/至多选 k 个：\(\sum x_i\ge/\le k\)；
- 二选一：\(x_1+x_2=1\)；
- 前置关系：\(x_B\le x_A\)。

Big-M 必须尽量紧；过大会导致数值不稳定和松弛弱。能用 indicator constraint 时优先使用。

## 4. 非线性规划

目标或约束含非线性。区分：

- 凸优化：在凸可行域上最小化凸目标时，任意局部最优都是全局最优；仍需核对凸性、
  可行域与数值求解条件；
- 非凸优化：可能多局部最优，需多初值、全局界或启发式；
- 光滑/非光滑；
- 有约束/无约束。

报告初值、边界、容差、迭代次数、收敛状态，并检查可行性与 stationarity。
KKT 在一般非凸问题中通常只是必要条件，不能单独证明全局最优。

## 5. 动态规划

适合阶段决策与最优子结构。

要素：

- 状态 \(s_t\)；
- 决策 \(a_t\)；
- 状态转移 \(s_{t+1}=f(s_t,a_t)\)；
- 阶段成本 \(g_t\)；
- Bellman 方程。

\[
V_t(s)=\min_a\{g_t(s,a)+V_{t+1}(f(s,a))\}
\]

难点是状态爆炸。可用状态压缩、近似动态规划、滚动时域或强化学习。

## 6. 图与网络优化

- 最短路：Dijkstra（非负边）、Bellman–Ford（可负边）、Floyd（全源）；
- 最大流/最小割；
- 最小费用流；
- 二分图匹配/指派；
- 最小生成树；
- 车辆路径与旅行商。

若问题天然是网络结构，优先使用专门算法，通常比一般 MILP 更高效。

## 7. 多目标优化

冲突目标例如成本、服务、风险、碳排。

方法：

- 加权和：简单，但权重与量纲敏感，非凸 Pareto 前沿可能覆盖不全；
- ε-约束：优化主目标，其他目标设阈值；
- 目标规划：最小化偏离目标值；
- Pareto 演化算法：适合复杂非凸问题；
- 分层/词典序：目标有明确优先级。

输出不应只给一个“综合最优”，而应展示 Pareto 权衡和决策拐点。

## 8. 鲁棒与随机优化

### 鲁棒优化

参数位于不确定集合内，寻找对最坏合理情况仍可行/表现好的方案。
优点是可解释、无需精确分布；可能保守。

### 随机规划

已知或能估计场景概率，优化期望成本、风险或机会约束。
两阶段模型常写为“先决策—观察不确定性—补救决策”。

### 情景优化

构造低、中、高或历史重采样场景。报告：

- 场景覆盖；
- 场景概率来源；
- 期望表现；
- 最坏/分位表现；
- 可行概率。

## 9. 元启发式算法

遗传算法、粒子群、模拟退火、蚁群等适合复杂、非凸、组合问题，但不自动保证全局最优。

必须说明：

- 编码与解码；
- 如何保证/修复可行性；
- 适应度；
- 参数与停止条件；
- 多随机种子重复；
- 与贪心、小规模精确解、求解器下界比较；
- 最优值分布和运行时间。

不要因为不知道如何精确建模就直接套元启发式。

## 10. 优化结果验证

- 检查每条约束的最大违反量；
- 将解代回目标函数；
- 小规模实例与穷举/精确算法比较；
- 多初值、多种子；
- 参数敏感性与价格分析；
- 用仿真验证动态/随机环境表现；
- 与现行策略、简单启发式对比；
- 报告求解时间、gap 和停止原因。

## 11. 常见错误

- 把不可控量写成决策变量；
- 目标函数重复计算收益或成本；
- 逻辑约束缺失，解在数学上最优但现实不可执行；
- 单位不一致；
- Big-M 无依据；
- 用预测点值优化，却不考虑预测误差；
- 只报告算法最好一次结果；
- 把“求解器找到的解”直接称为全局最优。

## 12. 凸性检查

在使用一般非线性优化器前，先检查可行域和目标是否凸：

- 凸问题的局部最优也是全局最优；
- 二阶可微目标可检查 Hessian 半正定；
- 非凸问题要报告多初值、下界或全局求解证据；
- Newton 全步只在局部条件良好时有快速收敛，实际常需线搜索或信赖域。

Newton 方向通过解
\[
\nabla^2 f(x_k)d_k=-\nabla f(x_k)
\]
获得，不应显式求 Hessian 的逆。只有 Hessian 正定时，该方向才是局部二次模型的唯一极小点
且为下降方向；Hessian 可逆但不定时，方程的解可能是鞍点方向或上升方向。
使用修正 Hessian、阻尼 Newton 或信赖域，并报告最终梯度/KKT 残差。

对无约束凸函数，\(0\in\partial f(x^\*)\) 是全局最优的充分必要条件；有约束时应写成
\[
0\in\partial f(x^\*)+N_C(x^\*),
\]
其中 \(N_C\) 为可行集法锥。不能把无约束的“梯度等于零”直接用于边界最优。

Armijo、Goldstein、Wolfe 与强 Wolfe 是步长接受条件，不单独保证整个算法收敛；
还需要下降方向、光滑性、下界和角度/曲率等条件。经验常数（如 \(10^{-4}\)）是默认候选，
不是数学定理。

矩阵、凸性与最优性条件详见 `19_数学基础与统计理论补充.md`。
