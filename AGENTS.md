# 数学建模知识库执行约定

## 唯一入口

- `routing-manifest.yaml` 是机器路由的唯一手工维护事实源；
  `00B_任务路由与最小读取集.md` 由生成脚本产生，不得手工维护题型映射。
- 方法正文仍以本目录内对应专题为唯一知识源，不复制到 skill 或第二套 references。
- README、案例、复盘、来源台账和原始 `sources/` 不属于默认建模上下文。

## 新任务启动

1. 完整读取 `00Q_永久质量核心.md`。
2. 路由 ID 不明确时，先运行：
   `tools/route_knowledge.ps1 -ListRoutes`。
3. 根据任务目标、数据结构、响应支持集、当前阶段和风险选择最少必要的路由 ID 与阶段，再运行：
   `tools/route_knowledge.ps1 -RouteId <id...> -Stage <stage...>`。
4. 完整读取输出中的 `required` 文件；`optional` 只有在相应条件真实出现时才加载，
   `deferred` 不得提前默认加载，`forbidden` 不得作为默认方法证据。
5. 开始实质分析、计算或编码时，简要列出实际读取文件和采用的路由 ID。

文件、脚本或清单不可访问时，必须说明缺失项及影响；可采用可靠通用方法继续，但不得声称已读取，
也不得靠猜测恢复本库规则。禁止一次性加载整个知识库或整个 `sources/`。

## 阶段硬停止

- 读取、清洗、连接数据或做特征工程前，进入 `data` 阶段并执行数据门禁。
- 选择、拟合或比较模型前，进入 `model_selection` 与 `validation_design` 阶段；先冻结目标、
  响应支持集、依赖结构、简单基线、验证单位、外层划分和主指标。
- 需要编码和复现时进入 `implementation` 阶段。
- 首次正式比较模型、解释结果或形成结论前，进入 `validation_audit` 阶段并完整执行 `11`。
- 输出正式论文、报告、代码或竞赛提交物前进入 `delivery` 阶段。

未通过前一阶段硬门禁，不得以减少上下文为理由跳过，也不得声称相应工作已经完成。

## 回归任务

任何回归、回归预测、因果、面板、空间、生存、删失或机器学习回归任务，先使用回归路由并读取
`35_回归模型选择与方法总览.md` 的轻量选择门禁。只在短入口不足、需要完整公式/属性表，或当前
分支没有独立方法卡时读取 `35A_回归模型完整方法索引.md`。属性评分不能替代硬门禁、基线和训练外验证。

## 证据红线

执行 `00Q_永久质量核心.md`：不得编造数据、来源、参数、公式、实验结果或奖项；动态事实按任务日期
和版本复核；公式检查定义、条件、量纲、边界和实现一致性；启发式不得冒充全局最优；获奖论文只作案例。
用户明确要求不学习、不记录或不更新知识库时必须遵守。

## 知识库维护

知识库默认只读。只有用户明确授权后才可写入；写入前完整读取
`00C_知识条目与证据维护规范.md`，不得覆盖原始资料。

修改路由时只编辑 `routing-manifest.yaml`，随后运行：

1. `tools/generate_routing_docs.ps1`
2. `tools/test_routing.ps1`
3. `tools/validate_library.ps1`

任何修改都要更新 `CHANGELOG.md` 并运行全库校验。仅正文变化时不要求重新生成未变化的路由视图；
路由清单、路由脚本、生成视图或入口协议变化时必须执行以上三步。必须读取命令的完整输出后，
才能声称生成、测试或审计通过。
